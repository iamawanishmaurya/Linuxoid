---
phase: 10-jni-registration-dispatch
plan: "02"
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/native_execute_stub.cpp
  - include/wfa/native_execute_stub.hpp
  - src/apk_native_launch.cpp
  - include/wfa/apk_native_launch.hpp
  - tests/test_main.cpp
autonomous: true
requirements: [JNI-10]
user_setup: []
must_haves:
  truths:
    - "Once the registration-helper dispatch advances past the current seam, Linuxoid reports the first true post-registration managed bootstrap boundary exactly."
    - "JNI, registration dispatch, registration outcome, and later managed-start states stay distinct in launch JSON."
  artifacts:
    - path: "src/native_execute_stub.cpp"
      provides: "Exact post-registration seam"
    - path: "src/apk_native_launch.cpp"
      provides: "Operator-facing launch and first-app-start truth for the new seam"
  key_links:
    - from: "src/native_execute_stub.cpp"
      to: "src/apk_native_launch.cpp"
      via: "registration dispatch state -> first-app-start blocker propagation"
      pattern: "jni_registration_dispatch_required"
---

<objective>
Expose the first true post-registration startup boundary for the keyboard APK.

Purpose: Move Linuxoid from registration-required failure into an exact registration outcome or managed-bootstrap seam.
Output: Deterministic post-registration truth through `launch-apk` and `--first-app-start-proof`.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-jni-registration-dispatch/10-RESEARCH.md
@.planning/phases/10-jni-registration-dispatch/10-PATTERNS.md
@src/native_execute_stub.cpp
@include/wfa/native_execute_stub.hpp
@src/apk_native_launch.cpp
@include/wfa/apk_native_launch.hpp
@tests/test_main.cpp
</context>

<threat_model>
<trust_boundaries>
- Post-registration native startup and managed-bootstrap behavior crossing into user-visible diagnostics
</trust_boundaries>
<stride_register>
- threat_id: T-10-02
  category: spoofing
  component: registration-dispatch-diagnostics
  disposition: mitigate
  mitigation_plan: separate `JNI_OnLoad`, registration-helper selection, registration dispatch, registration outcome, and later managed-start states explicitly so Linuxoid cannot accidentally overclaim startup progress
</stride_register>
</threat_model>

<tasks>
<task type="auto">
  <name>Task 1: Surface the first post-registration startup boundary exactly</name>
  <files>src/native_execute_stub.cpp, include/wfa/native_execute_stub.hpp, src/apk_native_launch.cpp, include/wfa/apk_native_launch.hpp</files>
  <read_first>src/native_execute_stub.cpp, src/apk_native_launch.cpp, include/wfa/apk_native_launch.hpp, .planning/phases/10-jni-registration-dispatch/10-PATTERNS.md</read_first>
  <action>Extend the current native execute path so once the primary library loads, `JNI_OnLoad` succeeds, and Linuxoid dispatches the registration-helper boundary, Linuxoid exposes the next exact startup seam: registration outcome, managed bootstrap dependency, or a specific framework boundary. Keep the JSON stable and preserve the downstream managed blocker as a later seam, not a replacement for registration truth.</action>
  <verify>TMPDIR=/home/astra/codex/wine-for-android/.tmp cmake --build build && TMPDIR=/home/astra/codex/wine-for-android/.tmp ctest --test-dir build --output-on-failure</verify>
  <acceptance_criteria>
    - post-registration startup states stay distinct and machine-readable
    - `launch-apk --first-app-start-proof` reports the first true post-registration seam once dispatch starts
    - deterministic offline regression coverage pins the new boundary
  </acceptance_criteria>
  <done>Linuxoid exposes the first real post-registration startup boundary for the keyboard APK path.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `TMPDIR=/home/astra/codex/wine-for-android/.tmp cmake --build build`
- [ ] `TMPDIR=/home/astra/codex/wine-for-android/.tmp ctest --test-dir build --output-on-failure`
- [ ] Launch JSON distinguishes load success, `JNI_OnLoad`, registration-helper selection, registration dispatch, registration outcome, and later managed seams
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Linuxoid reaches the first true post-registration seam for the real keyboard APK path
</success_criteria>

<output>
After completion, create `.planning/phases/10-jni-registration-dispatch/10-02-SUMMARY.md`
</output>
