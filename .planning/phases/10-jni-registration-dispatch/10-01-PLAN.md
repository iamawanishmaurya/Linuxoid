---
phase: 10-jni-registration-dispatch
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/native_execute_stub.cpp
  - include/wfa/native_execute_stub.hpp
  - src/apk_native_launch.cpp
  - tests/test_main.cpp
autonomous: true
requirements: [JNI-09]
user_setup: []
must_haves:
  truths:
    - "Linuxoid no longer stops at the generic `jni_registration_dispatch_required` seam for `libjni_latinime.so`."
    - "The real keyboard APK path moves into a smaller exact registration execution seam."
  artifacts:
    - path: "src/native_execute_stub.cpp"
      provides: "Linuxoid-owned JNI registration dispatch attempt beyond the current seam"
    - path: "src/apk_native_launch.cpp"
      provides: "Operator-facing launch truth for the new registration seam"
  key_links:
    - from: "src/native_execute_stub.cpp"
      to: "src/apk_native_launch.cpp"
      via: "registration dispatch report and launch status"
      pattern: "jni_registration_dispatch_required"
---

<objective>
Close the first JNI registration dispatch gap for the real keyboard APK.

Purpose: Move Linuxoid from a discovered registration-helper boundary into a real registration dispatch attempt.
Output: A smaller, exact seam than the current `jni_registration_dispatch_required` boundary.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-jni-registration-dispatch/10-RESEARCH.md
@.planning/phases/10-jni-registration-dispatch/10-PATTERNS.md
@src/native_execute_stub.cpp
@include/wfa/native_execute_stub.hpp
@src/apk_native_launch.cpp
@tests/test_main.cpp
</context>

<threat_model>
<trust_boundaries>
- Untrusted staged `.so` payloads extracted from `/home/astra/Downloads/keyboard-0.1.28.apk`
- Linuxoid-owned JNI registration behavior reflected back into operator-facing JSON
</trust_boundaries>
<stride_register>
- threat_id: T-10-01
  category: tampering
  component: jni-registration-dispatch
  disposition: mitigate
  mitigation_plan: keep any JNI registration execution explicit, deterministic, and limited to the minimum surface needed to move the real keyboard seam
</stride_register>
</threat_model>

<tasks>
<task type="auto">
  <name>Task 1: Move beyond generic registration-required failure for `libjni_latinime.so`</name>
  <files>src/native_execute_stub.cpp, include/wfa/native_execute_stub.hpp, src/apk_native_launch.cpp, tests/test_main.cpp</files>
  <read_first>src/native_execute_stub.cpp, src/apk_native_launch.cpp, .planning/phases/10-jni-registration-dispatch/10-RESEARCH.md</read_first>
  <action>Extend or tighten the native execute path so a JNI-shaped primary library can advance beyond the current registration-required state into a Linuxoid-owned registration dispatch attempt or a smaller exact registration execution seam. Preserve deterministic reporting and do not claim app start success if Linuxoid still lacks the required dispatch bridge.</action>
  <verify>TMPDIR=/home/astra/codex/wine-for-android/.tmp cmake --build build</verify>
  <acceptance_criteria>
    - the real keyboard APK seam is smaller than the current `jni_registration_dispatch_required` blocker
    - `native_execute.*` reporting stays deterministic
    - fixture/registration coverage stays offline and green
  </acceptance_criteria>
  <done>Linuxoid reaches a smaller registration seam for the real keyboard APK.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `TMPDIR=/home/astra/codex/wine-for-android/.tmp cmake --build build`
- [ ] `TMPDIR=/home/astra/codex/wine-for-android/.tmp ctest --test-dir build --output-on-failure`
- [ ] Real or representative launch JSON shows a smaller app-start blocker than the current `jni_registration_dispatch_required` failure
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Linuxoid narrows the real keyboard APK registration seam without pretending full native or managed success
</success_criteria>

<output>
After completion, create `.planning/phases/10-jni-registration-dispatch/10-01-SUMMARY.md`
</output>
