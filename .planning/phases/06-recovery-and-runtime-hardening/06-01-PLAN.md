---
phase: 06-recovery-and-runtime-hardening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/apk_native_launch.cpp
  - include/wfa/apk_native_launch.hpp
  - src/apk_storage_bridge.cpp
  - include/wfa/apk_storage_bridge.hpp
  - src/apk_permission_bridge.cpp
  - include/wfa/apk_permission_bridge.hpp
  - tests/test_main.cpp
autonomous: true
requirements: [APP-01, APP-02]
user_setup: []
must_haves:
  truths:
    - "Repeated verification APK launches preserve and validate deterministic sandbox, permission, and AppOps artifacts under the same app-data root."
    - "Linuxoid keeps the exact upstream native blocker explicit while hardening repeated state continuity."
  artifacts:
    - path: "src/apk_storage_bridge.cpp"
      provides: "Repeated-run sandbox/app-data continuity"
    - path: "src/apk_permission_bridge.cpp"
      provides: "Repeated-run permission/AppOps continuity"
  key_links:
    - from: "src/apk_native_launch.cpp"
      to: "src/apk_storage_bridge.cpp"
      via: "staged_dir -> sandbox/data/data/<package> continuity"
      pattern: "storage"
    - from: "src/apk_native_launch.cpp"
      to: "src/apk_permission_bridge.cpp"
      via: "same sandbox root -> persisted permission/AppOps validation"
      pattern: "permissions"
---

<objective>
Harden sandbox, permission, and AppOps continuity for repeated verification APK launches.

Purpose: Phase 6 should make the first real keyboard app path repeatable by validating and preserving the same sandbox-backed app-data, permission, and AppOps artifacts across repeated runs instead of treating them as disposable byproducts.
Output: Deterministic repeated-run continuity through `launch-apk`, `--storage-proof`, and `--permissions-proof`.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-recovery-and-runtime-hardening/06-RESEARCH.md
@.planning/phases/06-recovery-and-runtime-hardening/06-PATTERNS.md
@src/apk_native_launch.cpp
@include/wfa/apk_native_launch.hpp
@src/apk_storage_bridge.cpp
@include/wfa/apk_storage_bridge.hpp
@src/apk_permission_bridge.cpp
@include/wfa/apk_permission_bridge.hpp
@tests/test_main.cpp
</context>

<threat_model>
<trust_boundaries>
- Repeated launch/session staging crossing into persisted sandbox/app-data state
- Persisted permission/AppOps state crossing into repeated launch readiness and diagnostics
</trust_boundaries>
<stride_register>
- threat_id: T-06-01
  category: tampering
  component: repeated-run-state-continuity
  disposition: mitigate
  mitigation_plan: validate and reuse the same sandbox-backed artifacts explicitly instead of overwriting them blindly on each launch
- threat_id: T-06-02
  category: denial_of_service
  component: stale-or-clobbered-contract-state
  disposition: mitigate
  mitigation_plan: preserve exact continuity or healing truth when sandbox, permission, or AppOps artifacts are missing, stale, malformed, or incompatible
</stride_register>
</threat_model>

<tasks>
<task type="auto">
  <name>Task 1: Make repeated keyboard APK launches validate and preserve sandbox/app-data continuity</name>
  <files>src/apk_native_launch.cpp, include/wfa/apk_native_launch.hpp, src/apk_storage_bridge.cpp, include/wfa/apk_storage_bridge.hpp</files>
  <read_first>src/apk_native_launch.cpp, src/apk_storage_bridge.cpp, include/wfa/apk_storage_bridge.hpp, .planning/phases/06-recovery-and-runtime-hardening/06-RESEARCH.md</read_first>
  <action>Extend the existing keyboard launch path so repeated runs against the same staging root validate and preserve the same sandbox/app-data artifacts intentionally. Surface continuity or healing truth explicitly without hiding the existing upstream native blocker.</action>
  <verify>TMPDIR=/home/astra/codex/wine-for-android/.tmp cmake --build build</verify>
  <acceptance_criteria>
    - repeated keyboard-like runs expose deterministic sandbox/app-data continuity or healing state
    - storage marker and artifact paths remain comparable across repeated runs
    - upstream native blockers remain explicit instead of collapsing into generic continuity output
  </acceptance_criteria>
  <done>Linuxoid treats sandbox/app-data continuity as a first-class part of the first real app path.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve and validate permission/AppOps continuity on the same repeated app path</name>
  <files>src/apk_native_launch.cpp, src/apk_permission_bridge.cpp, include/wfa/apk_permission_bridge.hpp, tests/test_main.cpp</files>
  <read_first>src/apk_native_launch.cpp, src/apk_permission_bridge.cpp, tests/test_main.cpp, .planning/phases/06-recovery-and-runtime-hardening/06-PATTERNS.md</read_first>
  <action>Harden the repeated-run path so persisted permission/AppOps artifacts are validated, reused, or healed explicitly under the same keyboard APK app-data root. Add deterministic regression coverage for repeated-run continuity and exact state truth.</action>
  <verify>TMPDIR=/home/astra/codex/wine-for-android/.tmp cmake --build build && TMPDIR=/home/astra/codex/wine-for-android/.tmp ctest --test-dir build --output-on-failure</verify>
  <acceptance_criteria>
    - repeated keyboard-like runs expose deterministic permission/AppOps continuity or healing state
    - permission/AppOps reports remain comparable across repeated runs
    - tests fail if repeated launches silently clobber or misreport persisted state
  </acceptance_criteria>
  <done>Linuxoid preserves app identity, sandbox, and permission/AppOps state coherently across repeated verification runs.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `TMPDIR=/home/astra/codex/wine-for-android/.tmp cmake --build build`
- [ ] `TMPDIR=/home/astra/codex/wine-for-android/.tmp ctest --test-dir build --output-on-failure`
- [ ] Repeated keyboard-like launches report deterministic sandbox, permission, and AppOps continuity or healing truth under the same app-data root
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Repeated verification runs preserve and validate the same sandbox-backed app-state story
</success_criteria>

<output>
After completion, create `.planning/phases/06-recovery-and-runtime-hardening/06-01-SUMMARY.md`
</output>
