---
phase: 06-recovery-and-runtime-hardening
plan: "02"
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/apk_self_healing_watchdog.cpp
  - include/wfa/apk_self_healing_watchdog.hpp
  - src/apk_native_launch.cpp
  - include/wfa/apk_native_launch.hpp
  - tests/test_main.cpp
  - README.md
  - CHANGELOG.md
  - docs/phased-build-plan.md
  - docs/self-healing-runtime-skeleton.md
  - docs/steps.md
  - src/project_status.cpp
autonomous: true
requirements: [VER-03]
user_setup: []
must_haves:
  truths:
    - "The Self-Healing Android Device runtime harness keeps recovery reporting rooted in the earliest true blocker on the first real keyboard app path."
    - "Repeated failed keyboard APK launches produce stable, comparable recovery artifacts instead of noisy downstream repair churn."
  artifacts:
    - path: "src/apk_self_healing_watchdog.cpp"
      provides: "Recovery gating, journal truth, and next-action accuracy"
    - path: "tests/test_main.cpp"
      provides: "Repeated-run watchdog and blocker regression coverage"
  key_links:
    - from: "src/apk_native_launch.cpp"
      to: "src/apk_self_healing_watchdog.cpp"
      via: "upstream native blocker -> recovery gating and journal output"
      pattern: "self_healing_android_device"
---

<objective>
Tighten Self-Healing Android Device recovery reporting for the first real app path.

Purpose: Phase 6 should make Linuxoid's watchdog and recovery artifacts actionable and comparable for repeated verification APK failures by keeping recovery output rooted in the earliest true blocker instead of downstream noise.
Output: Exact recovery gating, journal semantics, docs, and regression coverage through `launch-apk` and `--first-app-start-proof`.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-recovery-and-runtime-hardening/06-RESEARCH.md
@.planning/phases/06-recovery-and-runtime-hardening/06-PATTERNS.md
@src/apk_self_healing_watchdog.cpp
@include/wfa/apk_self_healing_watchdog.hpp
@src/apk_native_launch.cpp
@include/wfa/apk_native_launch.hpp
@tests/test_main.cpp
@README.md
@CHANGELOG.md
@docs/phased-build-plan.md
@docs/self-healing-runtime-skeleton.md
@docs/steps.md
@src/project_status.cpp
</context>

<threat_model>
<trust_boundaries>
- Upstream native blocker truth crossing into downstream recovery sequencing
- Watchdog journal output crossing into user-facing docs and status
</trust_boundaries>
<stride_register>
- threat_id: T-06-03
  category: tampering
  component: recovery-gating-truth
  disposition: mitigate
  mitigation_plan: gate or classify downstream repairs against the earliest launch blocker so Linuxoid does not imply recoverability it cannot yet deliver
- threat_id: T-06-04
  category: denial_of_service
  component: repeated-run-recovery-noise
  disposition: mitigate
  mitigation_plan: make repeated failures comparable through stable action ordering, precise reasons, and exact recommended next actions
</stride_register>
</threat_model>

<tasks>
<task type="auto">
  <name>Task 1: Gate watchdog recovery against the earliest upstream blocker</name>
  <files>src/apk_self_healing_watchdog.cpp, include/wfa/apk_self_healing_watchdog.hpp, src/apk_native_launch.cpp, include/wfa/apk_native_launch.hpp</files>
  <read_first>src/apk_self_healing_watchdog.cpp, src/apk_native_launch.cpp, include/wfa/apk_self_healing_watchdog.hpp, .planning/phases/06-recovery-and-runtime-hardening/06-RESEARCH.md</read_first>
  <action>Tighten the watchdog so blocked keyboard APK launches keep the upstream native seam authoritative. Downstream process/window/runtime repairs should be gated or classified explicitly when launch readiness is already blocked by native loading.</action>
  <verify>TMPDIR=/home/astra/codex/wine-for-android/.tmp cmake --build build</verify>
  <acceptance_criteria>
    - the earliest native blocker remains the authoritative reason on blocked keyboard launches
    - downstream recovery actions are suppressed or clearly gated when they cannot succeed before native load is fixed
    - journal output stays deterministic and comparable across repeated blocked runs
  </acceptance_criteria>
  <done>Linuxoid's watchdog recovery story matches the actual first real app blocker chain.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the hardening checkpoint into tests, status, and repo truth</name>
  <files>tests/test_main.cpp, README.md, CHANGELOG.md, docs/phased-build-plan.md, docs/self-healing-runtime-skeleton.md, docs/steps.md, src/project_status.cpp</files>
  <read_first>tests/test_main.cpp, README.md, src/project_status.cpp, .planning/phases/06-recovery-and-runtime-hardening/06-PATTERNS.md</read_first>
  <action>Add deterministic regression coverage for repeated-run continuity and exact recovery gating, then update docs/status so the hardening checkpoint says plainly what Linuxoid now preserves, what the watchdog now reports, and what exact native or managed blocker still remains.</action>
  <verify>TMPDIR=/home/astra/codex/wine-for-android/.tmp cmake --build build && TMPDIR=/home/astra/codex/wine-for-android/.tmp ctest --test-dir build --output-on-failure</verify>
  <acceptance_criteria>
    - tests fail if repeated-run continuity or exact recovery gating regresses
    - docs/status reflect the same blocker and recovery semantics the CLI emits
    - the next blocker after the hardening checkpoint stays explicit
  </acceptance_criteria>
  <done>The hardening checkpoint is durable in code, tests, and repo truth.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `TMPDIR=/home/astra/codex/wine-for-android/.tmp cmake --build build`
- [ ] `TMPDIR=/home/astra/codex/wine-for-android/.tmp ctest --test-dir build --output-on-failure`
- [ ] Repeated keyboard APK failures preserve the exact upstream blocker and produce stable, comparable recovery artifacts
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The Self-Healing Android Device runtime harness emits tighter, more actionable, and more comparable recovery truth for the first real app path
</success_criteria>

<output>
After completion, create `.planning/phases/06-recovery-and-runtime-hardening/06-02-SUMMARY.md`
</output>
