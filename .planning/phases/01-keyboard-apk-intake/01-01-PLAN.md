---
phase: "01"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "src/apk_native_launch.cpp"
  - "include/wfa/apk_native_launch.hpp"
  - "src/main.cpp"
  - "tests/test_main.cpp"
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Linuxoid can extract trustworthy package, launcher, permission, and ABI facts from the real keyboard APK or report the exact binary-manifest blocker."
    - "Phase-1 intake stays inside the existing compatctl direct APK path."
  artifacts:
    - path: "src/apk_native_launch.cpp"
      provides: "Real-APK manifest and metadata intake through the staged launch path"
    - path: "tests/test_main.cpp"
      provides: "Deterministic regression coverage for real-APK intake seams"
  key_links:
    - from: "src/main.cpp"
      to: "src/apk_native_launch.cpp"
      via: "compatctl launch-apk"
      pattern: "launch-apk"
    - from: "src/apk_native_launch.cpp"
      to: "include/wfa/apk_native_launch.hpp"
      via: "NativeApkLaunchReport"
      pattern: "NativeApkLaunchReport"
---

# Phase 1 Plan 01: Harden real-world APK manifest and metadata intake

## Objective
- **What:** Teach Linuxoid's existing direct APK path to extract the keyboard target's core manifest/package/component facts or stop at a precise binary-manifest blocker.
- **Why:** Later execution phases are meaningless unless the real verification APK can be identified and staged truthfully.
- **Output:** Trustworthy package/activity/permission/ABI metadata flowing through the current launch/report contract.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-keyboard-apk-intake/01-CONTEXT.md
@.planning/phases/01-keyboard-apk-intake/01-RESEARCH.md

## Tasks

<task type="code">
  <name>Characterize the real keyboard APK intake failure boundary</name>
  <files>src/apk_native_launch.cpp, tests/test_main.cpp</files>
  <action>Run the current direct APK path against `/home/astra/Downloads/keyboard-0.1.28.apk`, identify whether the blocker is binary manifest parsing, large-APK enumeration, or another intake seam, and turn that boundary into deterministic code/test coverage.</action>
  <verify>Existing tests still pass and the real-APK smoke path either returns the expected metadata or a precise structured blocker.</verify>
  <done>The repo contains a stable regression for the exact intake boundary currently hit by the keyboard APK.</done>
</task>

<task type="code">
  <name>Add the smallest real-APK manifest metadata bridge</name>
  <files>src/apk_native_launch.cpp, include/wfa/apk_native_launch.hpp, src/main.cpp</files>
  <action>Implement the smallest Linuxoid-owned manifest intake slice needed to surface package name, launcher activity, permission list, and ABI inventory for the verification APK through the existing launch/report path.</action>
  <verify>`compatctl launch-apk` and/or a focused inspection surface can report those facts for deterministic fixtures and the real keyboard APK when present.</verify>
  <done>Core manifest/package/component facts no longer depend on decoded plain-text fixtures alone.</done>
</task>

## Verification
Validate the launch/report JSON against a deterministic fixture plus the local keyboard APK when present. Package name, launcher component, permissions, and ABI/native inventory must be correct or blocked with an explicit reason.

## Success Criteria
- [ ] Real-APK intake reaches trustworthy package and launcher metadata or returns a precise binary-manifest blocker
- [ ] Permission and ABI/native inventory are surfaced through existing Linuxoid reports
- [ ] Tests pin the boundary so later phases build on real APK truth instead of fixture assumptions
