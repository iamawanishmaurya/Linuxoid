---
phase: "01"
plan: "02"
type: "execute"
wave: 2
depends_on:
  - "01-01"
files_modified:
  - "src/apk_native_launch.cpp"
  - "src/apk_permission_bridge.cpp"
  - "src/apk_asset_bridge.cpp"
  - "tests/test_main.cpp"
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Linuxoid stages the real keyboard APK into a deterministic session root without emulator or Waydroid dependencies."
    - "Assets, permissions, and native-library inventory for the real APK are visible through existing Linuxoid proof surfaces."
  artifacts:
    - path: "src/apk_asset_bridge.cpp"
      provides: "Real-APK asset/resource inventory improvements if needed"
    - path: "src/apk_permission_bridge.cpp"
      provides: "Real-APK permission inventory reuse through the direct session path"
  key_links:
    - from: "src/apk_native_launch.cpp"
      to: "src/apk_asset_bridge.cpp"
      via: "resource and asset inventory"
      pattern: "asset_bridge"
    - from: "src/apk_native_launch.cpp"
      to: "src/apk_permission_bridge.cpp"
      via: "permission reporting"
      pattern: "permissions"
---

# Phase 1 Plan 02: Validate staging, assets, permissions, and native library inventory

## Objective
- **What:** Make the real verification APK's staged session root carry the intake facts later phases need.
- **Why:** Package metadata alone is not enough; the runtime needs staged permissions, resources, and native-library inventory that match the real APK.
- **Output:** Deterministic staging/report artifacts for the keyboard APK's assets, permissions, and native libraries.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-keyboard-apk-intake/01-CONTEXT.md
@.planning/phases/01-keyboard-apk-intake/01-RESEARCH.md
@.planning/phases/01-keyboard-apk-intake/01-01-PLAN.md

## Tasks

<task type="code">
  <name>Push real-APK staging through the existing session root</name>
  <files>src/apk_native_launch.cpp, tests/test_main.cpp</files>
  <action>Ensure the keyboard APK stages cleanly into the existing package/session layout and that staged metadata artifacts survive later proof modes.</action>
  <verify>The staging root contains deterministic package/session outputs for the real APK path without new external runtime dependencies.</verify>
  <done>The real APK uses the same Linuxoid session layout as existing fixture-driven proofs.</done>
</task>

<task type="code">
  <name>Expose real-APK permission, resource, and native-library inventory</name>
  <files>src/apk_permission_bridge.cpp, src/apk_asset_bridge.cpp, src/apk_native_launch.cpp, tests/test_main.cpp</files>
  <action>Reuse existing bridges to surface permission inventory, asset/resource metadata, and native-library ABI coverage from the real keyboard APK through structured JSON and staged artifacts.</action>
  <verify>The repo can show correct permission names, resource/asset visibility, and x86_64 library inventory for deterministic fixtures and the real APK smoke target.</verify>
  <done>Later execution phases can consume trustworthy staged metadata instead of assuming synthetic fixture coverage generalizes to the keyboard APK.</done>
</task>

## Verification
Use deterministic tests plus an opt-in local smoke path for `/home/astra/Downloads/keyboard-0.1.28.apk`. The staged session root and JSON reports must agree on permissions, assets/resources visibility, and native-library ABI inventory.

## Success Criteria
- [ ] The real APK stages into Linuxoid's deterministic session root
- [ ] Permissions, resource/asset metadata, and native-library inventory are visible through existing proof/report seams
- [ ] No emulator, Waydroid, ADB, or Android SDK dependency is introduced for the normal runtime path
