---
phase: 05-visible-wayland-interaction
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/apk_native_launch.cpp
  - include/wfa/apk_native_launch.hpp
  - src/apk_window_bridge.cpp
  - include/wfa/apk_window_bridge.hpp
  - src/native_window_surface.cpp
  - tests/test_main.cpp
autonomous: true
requirements: [WIN-01]
user_setup: []
must_haves:
  truths:
    - "Linuxoid binds the real `org.futo.inputmethod.latin/.uix.settings.SettingsActivity` session to one deterministic surface/window target."
    - "The real keyboard APK preserves the exact upstream native blocker when that blocker prevents visible launch."
  artifacts:
    - path: "src/apk_native_launch.cpp"
      provides: "Real keyboard window/surface proof propagation"
    - path: "src/apk_window_bridge.cpp"
      provides: "Persisted visible-launch window/session contract"
  key_links:
    - from: "src/apk_native_launch.cpp"
      to: "src/apk_window_bridge.cpp"
      via: "resolved activity/process/session -> window target mapping"
      pattern: "window_manager"
---

<objective>
Bind the real settings-activity session to Linuxoid's surface/window target path.

Purpose: Phase 5 should move the verification APK one step closer to a visible Linux launch by making the resolved `SettingsActivity` session own a concrete surface/window target contract, while keeping the upstream native blocker explicit when visible launch cannot proceed yet.
Output: Deterministic visible-target metadata through `launch-apk --window-proof` and `inspect-apk-window`.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-jni-and-native-loading/04-02-SUMMARY.md
@.planning/phases/05-visible-wayland-interaction/05-RESEARCH.md
@.planning/phases/05-visible-wayland-interaction/05-PATTERNS.md
@src/apk_native_launch.cpp
@include/wfa/apk_native_launch.hpp
@src/apk_window_bridge.cpp
@include/wfa/apk_window_bridge.hpp
@src/native_window_surface.cpp
@tests/test_main.cpp
</context>

<threat_model>
<trust_boundaries>
- Real APK launch truth crossing into persisted window/session state
- Optional live Wayland/EGL availability crossing into deterministic CLI output
</trust_boundaries>
<stride_register>
- threat_id: T-05-01
  category: tampering
  component: visible-launch-session-mapping
  disposition: mitigate
  mitigation_plan: keep the surface/window target tied to the resolved package/activity/process identity instead of a second synthetic display-only contract
- threat_id: T-05-02
  category: information_disclosure
  component: visible-launch-status
  disposition: mitigate
  mitigation_plan: distinguish live-available, probe-only, headless fallback, and native-blocked states explicitly so Linuxoid does not overclaim visible launch
</stride_register>
</threat_model>

<tasks>
<task type="auto">
  <name>Task 1: Bind the resolved SettingsActivity session to a concrete window/surface target</name>
  <files>src/apk_native_launch.cpp, include/wfa/apk_native_launch.hpp, src/apk_window_bridge.cpp, include/wfa/apk_window_bridge.hpp</files>
  <read_first>src/apk_native_launch.cpp, src/apk_window_bridge.cpp, include/wfa/apk_window_bridge.hpp, .planning/phases/05-visible-wayland-interaction/05-RESEARCH.md</read_first>
  <action>Extend the existing keyboard `--window-proof` path so Linuxoid records the real `SettingsActivity` target, one concrete surface/window target state, and whether that target is visible, blocked, or headless-only. Preserve the exact Phase 4 native blocker when launch readiness still prevents a visible window.</action>
  <verify>cmake --build build</verify>
  <acceptance_criteria>
    - `launch-apk --window-proof --package org.futo.inputmethod.latin --component org.futo.inputmethod.latin/.uix.settings.SettingsActivity ...` reports a concrete target state tied to the real activity session
    - upstream native blockers remain explicit instead of collapsing into a generic `window_manager_not_ready`
    - existing deterministic fixture window-proof behavior stays green
  </acceptance_criteria>
  <done>The real verification activity owns a concrete Linuxoid window/surface target contract.</done>
</task>

<task type="auto">
  <name>Task 2: Expose best-effort live Wayland/EGL target truth without breaking headless determinism</name>
  <files>src/native_window_surface.cpp, src/apk_native_launch.cpp, tests/test_main.cpp</files>
  <read_first>src/native_window_surface.cpp, src/apk_native_launch.cpp, tests/test_main.cpp, .planning/phases/05-visible-wayland-interaction/05-PATTERNS.md</read_first>
  <action>Reuse the optional Wayland/EGL probes so the window-proof path can describe whether a live target is available, probe-only, or headless fallback. Keep tests deterministic without a display and keep the real keyboard APK path honest if native launch still blocks before visible launch can continue.</action>
  <verify>cmake --build build && ctest --test-dir build --output-on-failure</verify>
  <acceptance_criteria>
    - live-probe availability stays explicit and optional
    - headless fallback remains deterministic in tests
    - operator-facing JSON distinguishes visible-target capability from actual app-start success
  </acceptance_criteria>
  <done>Linuxoid can describe a visible-launch target honestly on both live and headless hosts.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `cmake --build build`
- [ ] `ctest --test-dir build --output-on-failure`
- [ ] Real or representative `--window-proof` output reports a concrete window/surface target and preserves the exact upstream native blocker when launch is not ready
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Linuxoid reports a concrete visible-launch target for the real verification activity without overstating success
</success_criteria>

<output>
After completion, create `.planning/phases/05-visible-wayland-interaction/05-01-SUMMARY.md`
</output>
