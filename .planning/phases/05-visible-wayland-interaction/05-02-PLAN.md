---
phase: 05-visible-wayland-interaction
plan: "02"
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/apk_native_launch.cpp
  - include/wfa/apk_native_launch.hpp
  - src/apk_window_bridge.cpp
  - src/native_input_queue_fixture.cpp
  - tests/test_main.cpp
autonomous: true
requirements: [WIN-02]
user_setup: []
must_haves:
  truths:
    - "Linuxoid ties focus and meaningful input ownership to the same visible-launch session as the resolved verification activity."
    - "If focus or interaction cannot proceed, the blocker stays exact and does not erase the upstream native seam."
  artifacts:
    - path: "src/apk_native_launch.cpp"
      provides: "Focus/input/operator-facing proof propagation"
    - path: "src/native_input_queue_fixture.cpp"
      provides: "Deterministic focus ownership pattern reused by the app-start path"
  key_links:
    - from: "src/native_input_queue_fixture.cpp"
      to: "src/apk_window_bridge.cpp"
      via: "focus owner and interaction target continuity"
      pattern: "focus_owner"
---

<objective>
Make focus and interaction meaningful for the first visible-launch path.

Purpose: Once the real verification activity is bound to a surface/window target, Linuxoid should prove that the same session can own focus and accept meaningful input on the first milestone path, while still preserving exact blockers when upstream native or launch readiness prevents that interaction.
Output: Deterministic focus and interaction state through `launch-apk --window-proof` and related proof surfaces.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-visible-wayland-interaction/05-RESEARCH.md
@.planning/phases/05-visible-wayland-interaction/05-PATTERNS.md
@.planning/phases/05-visible-wayland-interaction/05-01-PLAN.md
@src/apk_native_launch.cpp
@include/wfa/apk_native_launch.hpp
@src/apk_window_bridge.cpp
@src/native_input_queue_fixture.cpp
@tests/test_main.cpp
</context>

<threat_model>
<trust_boundaries>
- Focus/input ownership crossing from fixtures into real app-session reporting
- Blocked launch/session state crossing into interaction readiness
</trust_boundaries>
<stride_register>
- threat_id: T-05-03
  category: tampering
  component: focus-owner-state
  disposition: mitigate
  mitigation_plan: tie focus ownership to the same package/activity/process/window identity already used by the visible-launch contract
- threat_id: T-05-04
  category: denial_of_service
  component: blocked-interaction-reporting
  disposition: mitigate
  mitigation_plan: preserve exact upstream blockers and distinguish blocked input from available-but-unfocused input
</stride_register>
</threat_model>

<tasks>
<task type="auto">
  <name>Task 1: Attach focus ownership to the visible-launch session</name>
  <files>src/apk_native_launch.cpp, src/apk_window_bridge.cpp, src/native_input_queue_fixture.cpp</files>
  <read_first>src/apk_native_launch.cpp, src/apk_window_bridge.cpp, src/native_input_queue_fixture.cpp, .planning/phases/05-visible-wayland-interaction/05-RESEARCH.md</read_first>
  <action>Extend the window-proof path so focus ownership and meaningful input target state belong to the same package/activity/process/window session as the resolved `SettingsActivity`. Reuse the deterministic native input queue focus-owner pattern rather than inventing a new interaction layer.</action>
  <verify>cmake --build build</verify>
  <acceptance_criteria>
    - focus/input ownership is tied to the same window/session IDs as the real verification activity target
    - blocked launch or blocked native load remains visible as the upstream cause when focus cannot be acquired
    - existing headless fixture focus behavior stays deterministic
  </acceptance_criteria>
  <done>Linuxoid has one coherent visible-launch and focus/input session story.</done>
</task>

<task type="auto">
  <name>Task 2: Carry exact interaction blockers through operator-facing proof output</name>
  <files>src/apk_native_launch.cpp, include/wfa/apk_native_launch.hpp, tests/test_main.cpp</files>
  <read_first>src/apk_native_launch.cpp, include/wfa/apk_native_launch.hpp, tests/test_main.cpp, .planning/phases/05-visible-wayland-interaction/05-PATTERNS.md</read_first>
  <action>Make the CLI proof output distinguish focused/interactive, headless-only, blocked-by-native, and blocked-by-launch states cleanly. Keep the result honest: visible interaction can advance only as far as the real verification app session allows on this host.</action>
  <verify>cmake --build build && ctest --test-dir build --output-on-failure</verify>
  <acceptance_criteria>
    - operator-facing JSON reports meaningful focus/input state or an exact blocker
    - the upstream native blocker is still preserved when it prevents interaction
    - the visible-launch contract does not imply full app usability before it exists
  </acceptance_criteria>
  <done>The first visible interaction seam is precise and machine-readable.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `cmake --build build`
- [ ] `ctest --test-dir build --output-on-failure`
- [ ] Representative proof output distinguishes focus/input readiness from native-blocked or headless-only states
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Linuxoid reports one coherent visible-launch plus focus/input story for the verification app path
</success_criteria>

<output>
After completion, create `.planning/phases/05-visible-wayland-interaction/05-02-SUMMARY.md`
</output>
