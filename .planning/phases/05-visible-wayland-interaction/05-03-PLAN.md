---
phase: 05-visible-wayland-interaction
plan: "03"
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - tests/test_main.cpp
  - README.md
  - CHANGELOG.md
  - docs/phased-build-plan.md
  - docs/self-healing-runtime-skeleton.md
  - docs/steps.md
  - src/project_status.cpp
autonomous: true
requirements: [VER-01]
user_setup: []
must_haves:
  truths:
    - "Visible-launch and interaction checkpoints are durable in tests and docs."
    - "Repo truth stays explicit about what is visibly interactive, what is still headless-only, and what the exact remaining blocker is."
  artifacts:
    - path: "tests/test_main.cpp"
      provides: "Visible-launch and focus/input regression coverage"
    - path: "README.md"
      provides: "Operator-facing checkpoint and next-blocker truth"
  key_links:
    - from: "tests/test_main.cpp"
      to: "README.md"
      via: "exact visible-launch and blocker semantics"
      pattern: "window-proof"
---

<objective>
Lock the visible-launch checkpoint into regression coverage and repo truth.

Purpose: Once Phase 5 narrows or advances the visible-launch seam, Linuxoid should not be able to silently regress back to vague window/surface or focus/input output.
Output: Stable tests, status, and docs for the visible-launch checkpoint.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-visible-wayland-interaction/05-RESEARCH.md
@.planning/phases/05-visible-wayland-interaction/05-PATTERNS.md
@tests/test_main.cpp
@README.md
@CHANGELOG.md
@docs/phased-build-plan.md
@docs/self-healing-runtime-skeleton.md
@docs/steps.md
@src/project_status.cpp
</context>

<threat_model>
<trust_boundaries>
- User-visible docs and status output mirroring low-level surface/focus/native truths
- Regression expectations that must stay aligned with live proof surfaces
</trust_boundaries>
<stride_register>
- threat_id: T-05-05
  category: tampering
  component: repo-truth
  disposition: mitigate
  mitigation_plan: document exactly the same visible-launch and blocker semantics that the CLI emits
- threat_id: T-05-06
  category: denial_of_service
  component: regression-gap
  disposition: mitigate
  mitigation_plan: pin the visible-launch seam in deterministic tests so it cannot silently collapse back into generic window-blocked output
</stride_register>
</threat_model>

<tasks>
<task type="auto">
  <name>Task 1: Add deterministic regression coverage for visible-launch and interaction state</name>
  <files>tests/test_main.cpp</files>
  <read_first>tests/test_main.cpp, .planning/phases/05-visible-wayland-interaction/05-PATTERNS.md</read_first>
  <action>Add or update regression coverage that keeps fixture-based visible-launch and focus/input seams deterministic while asserting that the real keyboard-like path preserves the exact native blocker when visible launch is still blocked.</action>
  <verify>cmake --build build && ctest --test-dir build --output-on-failure</verify>
  <acceptance_criteria>
    - tests fail if visible-launch truth collapses back into ambiguity
    - tests fail if the upstream native blocker is lost from the real keyboard-like path
    - tests remain offline and headless-safe
  </acceptance_criteria>
  <done>The visible-launch checkpoint is durable in regression coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Update repo truth and status around the visible-launch checkpoint</name>
  <files>README.md, CHANGELOG.md, docs/phased-build-plan.md, docs/self-healing-runtime-skeleton.md, docs/steps.md, src/project_status.cpp</files>
  <read_first>README.md, docs/phased-build-plan.md, src/project_status.cpp, .planning/phases/05-visible-wayland-interaction/05-RESEARCH.md</read_first>
  <action>Refresh operator-facing docs and status output so Phase 5 says plainly what is visible, what is only probe/headless-safe, and what exact blocker still prevents the real keyboard APK from behaving like a fully running Android app on Linux.</action>
  <verify>cmake --build build && ctest --test-dir build --output-on-failure</verify>
  <acceptance_criteria>
    - README/docs/status match the actual visible-launch proof surface
    - repo truth keeps the execution-first and exact-blocker language
    - the next blocker after the visible-launch checkpoint is explicit
  </acceptance_criteria>
  <done>The repo truth matches the visible-launch checkpoint exactly.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `cmake --build build`
- [ ] `ctest --test-dir build --output-on-failure`
- [ ] Docs and status agree with the exact visible-launch and blocker fields emitted by the CLI
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Visible-launch and blocker semantics are durable in tests and repo truth
</success_criteria>

<output>
After completion, create `.planning/phases/05-visible-wayland-interaction/05-03-SUMMARY.md`
</output>
