---
phase: 03-runtime-context-bridge
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/apk_dex_bridge.cpp
  - include/wfa/apk_dex_bridge.hpp
  - src/apk_native_launch.cpp
  - include/wfa/apk_native_launch.hpp
  - tests/test_main.cpp
autonomous: true
requirements: [DEX-02, VER-02]
user_setup: []
must_haves:
  truths:
    - "Linuxoid can carry the lifecycle receiver and invoke argument state one step farther than the current `invoke_receiver_missing` seam."
    - "The first-app-start report states exactly whether the receiver/invoke boundary was propagated, placeholder-modeled, or still blocked."
  artifacts:
    - path: "src/apk_dex_bridge.cpp"
      provides: "Receiver and invoke-state propagation through the first framework boundary"
    - path: "src/apk_native_launch.cpp"
      provides: "First-app-start proof fields for receiver/invoke-state diagnostics"
  key_links:
    - from: "src/apk_dex_bridge.cpp"
      to: "src/apk_native_launch.cpp"
      via: "execution probe receiver and invoke-state fields"
      pattern: "framework_boundary_reason"
---

<objective>
Advance Linuxoid past the current `invoke_receiver_missing` seam by carrying the receiver and minimal invoke argument state through the first framework-style call boundary.

Purpose: Phase 3 should replace one exact blocker with one smaller exact blocker instead of broadening runtime scope.
Output: A deeper managed-runtime seam with deterministic receiver/invoke-state reporting and a narrower post-receiver blocker.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-managed-activity-start/02-03-SUMMARY.md
@.planning/phases/03-runtime-context-bridge/03-RESEARCH.md
@.planning/phases/03-runtime-context-bridge/03-PATTERNS.md
@src/apk_dex_bridge.cpp
@src/apk_native_launch.cpp
@tests/test_main.cpp
</context>

<threat_model>
<trust_boundaries>
- Untrusted staged `classes.dex` contents and invoke register metadata
- Report JSON consumed by later execution and recovery steps
</trust_boundaries>
<stride_register>
- threat_id: T-03-01
  category: tampering
  component: invoke-state-propagation
  disposition: mitigate
  mitigation_plan: carry receiver and argument register state only through bounds-checked, deterministic interpreter paths and emit an exact blocker when propagation cannot continue
- threat_id: T-03-02
  category: denial-of-service
  component: interpreter-state
  disposition: mitigate
  mitigation_plan: keep receiver, argument, and pending-result bookkeeping explicit and fail closed with exact blocker strings rather than generic execution collapse
</stride_register>
</threat_model>

<tasks>
<task type="auto">
  <name>Task 1: Propagate receiver and invoke argument state across the first framework boundary</name>
  <files>src/apk_dex_bridge.cpp, include/wfa/apk_dex_bridge.hpp</files>
  <read_first>src/apk_dex_bridge.cpp, include/wfa/apk_dex_bridge.hpp, .planning/phases/03-runtime-context-bridge/03-RESEARCH.md</read_first>
  <action>Extend the minimal interpreter so the synthetic keyboard-identity lifecycle path can carry its receiver and minimal invoke-argument state through the current `invoke_receiver_missing` boundary. Add any new deterministic probe fields needed to report whether the receiver was propagated, placeholder-modeled, or blocked, and preserve exact post-receiver blocker strings when the runtime still cannot continue.</action>
  <verify>cmake --build build</verify>
  <acceptance_criteria>
    - `src/apk_dex_bridge.cpp` no longer stops at a generic receiver-missing boundary for the targeted synthetic seam
    - new receiver/invoke-state fields are deterministic and exact
    - the next blocker after receiver propagation is explicit rather than vague
  </acceptance_criteria>
  <done>Linuxoid reaches one smaller managed-runtime blocker beyond `invoke_receiver_missing`.</done>
</task>

<task type="auto">
  <name>Task 2: Surface receiver and invoke-state diagnostics through first-app-start JSON</name>
  <files>src/apk_native_launch.cpp, include/wfa/apk_native_launch.hpp, tests/test_main.cpp</files>
  <read_first>src/apk_native_launch.cpp, include/wfa/apk_native_launch.hpp, tests/test_main.cpp, .planning/phases/03-runtime-context-bridge/03-PATTERNS.md</read_first>
  <action>Thread the new receiver/invoke-state fields into `first_android_app_start`, keep the real keyboard APK lookup truth visible alongside the deeper synthetic managed seam, and add regression coverage that proves the runtime now reports a post-receiver blocker instead of the old `invoke_receiver_missing` seam when the targeted fixture path runs.</action>
  <verify>cmake --build build && ctest --test-dir build --output-on-failure</verify>
  <acceptance_criteria>
    - `RenderFirstAppStartJson` emits receiver/invoke-state truth deterministically
    - `tests/test_main.cpp` pins the new post-receiver blocker or successful propagation state
    - the real keyboard APK path still reports its upstream native/surface blocker honestly
  </acceptance_criteria>
  <done>The operator surface now exposes the narrowed managed-runtime seam clearly.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `cmake --build build`
- [ ] `ctest --test-dir build --output-on-failure`
- [ ] The targeted synthetic managed seam reports a post-receiver blocker or successful propagation instead of `invoke_receiver_missing`
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The next managed-runtime seam is smaller and more actionable than `invoke_receiver_missing`
</success_criteria>

<output>
After completion, create `.planning/phases/03-runtime-context-bridge/03-01-SUMMARY.md`
</output>
