---
phase: 03-runtime-context-bridge
plan: "03"
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - tests/test_main.cpp
  - README.md
  - CHANGELOG.md
  - docs/phased-build-plan.md
  - docs/self-healing-runtime-skeleton.md
  - docs/steps.md
  - src/project_status.cpp
autonomous: true
requirements: [VER-02]
user_setup: []
must_haves:
  truths:
    - "Phase 3 leaves a stable CLI proof for the narrowed managed-runtime context seam."
    - "Repo truth names the exact post-receiver blocker and still preserves the real keyboard APK's upstream native/surface blockers."
  artifacts:
    - path: "tests/test_main.cpp"
      provides: "Hard regression coverage for the narrowed runtime-context seam"
    - path: "README.md"
      provides: "Updated repo truth for the Phase 3 checkpoint and next blocker"
  key_links:
    - from: "tests/test_main.cpp"
      to: "src/apk_native_launch.cpp"
      via: "first-app-start proof regression"
      pattern: "next_blocker"
---

<objective>
Lock the narrowed runtime-context seam into regression coverage, CLI proof output, and repo truth.

Purpose: Phase 4 must inherit one stable managed-runtime blocker, not a moving development observation.
Output: Hard regression coverage plus docs/status updates that name the exact post-receiver seam and blocker honestly.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-runtime-context-bridge/03-RESEARCH.md
@.planning/phases/03-runtime-context-bridge/03-PATTERNS.md
@.planning/phases/03-runtime-context-bridge/03-01-PLAN.md
@.planning/phases/03-runtime-context-bridge/03-02-PLAN.md
@README.md
@CHANGELOG.md
@docs/phased-build-plan.md
@docs/self-healing-runtime-skeleton.md
@docs/steps.md
@src/project_status.cpp
@tests/test_main.cpp
</context>

<threat_model>
<trust_boundaries>
- Human-readable docs and status output that later work may treat as runtime truth
- Real APK smoke coverage that is opt-in and local-only
</trust_boundaries>
<stride_register>
- threat_id: T-03-05
  category: spoofing
  component: repo-truth
  disposition: mitigate
  mitigation_plan: update docs only from verified proof fields and exact blocker strings rather than speculative ART/runtime claims
- threat_id: T-03-06
  category: tampering
  component: regression-surface
  disposition: mitigate
  mitigation_plan: add assertions for exact post-receiver seam and blocker fields so future phases cannot silently regress the checkpoint
</stride_register>
</threat_model>

<tasks>
<task type="auto">
  <name>Task 1: Add hard regression coverage for the narrowed runtime-context seam</name>
  <files>tests/test_main.cpp</files>
  <read_first>tests/test_main.cpp, .planning/phases/03-runtime-context-bridge/03-RESEARCH.md</read_first>
  <action>Add focused regression coverage around `launch-apk --first-app-start-proof` for the synthetic managed seam after receiver propagation. Assert that Linuxoid reports the new post-receiver blocker or placeholder/stub state exactly, and that the real keyboard activity path still preserves the upstream `libraries_failed_to_load` plus `surface_not_ready_for_first_app_start` truth. Keep the tests deterministic and offline; any real-APK smoke must stay guarded and optional.</action>
  <verify>cmake --build build && ctest --test-dir build --output-on-failure</verify>
  <acceptance_criteria>
    - `tests/test_main.cpp` pins the narrowed post-receiver blocker or explicit placeholder/stub state
    - the real keyboard activity lookup truth remains covered
    - the full test suite remains green
  </acceptance_criteria>
  <done>The narrowed runtime-context seam is pinned by automated regression coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Update docs and status with the concrete Phase 3 checkpoint and next blocker</name>
  <files>README.md, CHANGELOG.md, docs/phased-build-plan.md, docs/self-healing-runtime-skeleton.md, docs/steps.md, src/project_status.cpp</files>
  <read_first>README.md, CHANGELOG.md, docs/phased-build-plan.md, docs/self-healing-runtime-skeleton.md, docs/steps.md, src/project_status.cpp</read_first>
  <action>Update repo truth so Phase 3 states exactly what the narrowed runtime-context seam now proves, what exact blocker remains next, and how that sits beside the upstream real keyboard APK native/surface blockers. Do not broaden the claim into full ART, ActivityThread, or JNI/native success if those are still blocked.</action>
  <verify>rg -n \"Runtime Context Bridge|next blocker|SettingsActivity|invoke_receiver|ActivityThread\" README.md CHANGELOG.md docs/phased-build-plan.md docs/self-healing-runtime-skeleton.md docs/steps.md src/project_status.cpp</verify>
  <acceptance_criteria>
    - `README.md` names the Phase 3 runtime-context checkpoint and its exact blocker
    - `CHANGELOG.md` records the concrete Phase 3 checkpoint, not a broad architecture claim
    - `src/project_status.cpp` summarizes the current managed-runtime seam truthfully
  </acceptance_criteria>
  <done>Phase 3 hands off a stable, auditable managed-runtime blocker into Phase 4.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `cmake --build build`
- [ ] `ctest --test-dir build --output-on-failure`
- [ ] Docs and status outputs agree on the same narrowed runtime-context seam and blocker
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repo truth and regression surface match the actual Phase 3 checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/03-runtime-context-bridge/03-03-SUMMARY.md`
</output>
