---
phase: 03-runtime-context-bridge
plan: "02"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/apk_dex_bridge.cpp
  - include/wfa/apk_dex_bridge.hpp
  - src/apk_native_launch.cpp
  - tests/test_main.cpp
autonomous: true
requirements: [DEX-02, VER-02]
user_setup: []
must_haves:
  truths:
    - "Linuxoid can distinguish receiver propagation from the next lifecycle-parameter, pending-result, or framework-owned callee blocker."
    - "The first-app-start proof exposes whether the next seam is placeholder-modeled, framework-stubbed, or still blocked."
  artifacts:
    - path: "src/apk_dex_bridge.cpp"
      provides: "Post-receiver lifecycle parameter and invoke-state diagnostics"
    - path: "tests/test_main.cpp"
      provides: "Regression coverage for the next runtime-context seam after receiver propagation"
  key_links:
    - from: "src/apk_dex_bridge.cpp"
      to: "src/apk_native_launch.cpp"
      via: "post-receiver blocker propagation"
      pattern: "next_blocker"
---

<objective>
Convert the first post-receiver runtime-context seam into an exact lifecycle parameter, pending-result, or framework-callee blocker.

Purpose: Phase 3 should end with a sharper managed-runtime handoff point than generic framework blocking.
Output: One deterministic post-receiver runtime-context seam with exact blocker reporting.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-runtime-context-bridge/03-RESEARCH.md
@.planning/phases/03-runtime-context-bridge/03-PATTERNS.md
@.planning/phases/03-runtime-context-bridge/03-01-PLAN.md
@src/apk_dex_bridge.cpp
@src/apk_native_launch.cpp
@tests/test_main.cpp
</context>

<threat_model>
<trust_boundaries>
- Untrusted staged lifecycle method bytecode and invoke metadata
- Persisted report JSON used by later ART and JNI phases
</trust_boundaries>
<stride_register>
- threat_id: T-03-03
  category: tampering
  component: lifecycle-parameter-state
  disposition: mitigate
  mitigation_plan: model lifecycle parameters or pending results only through explicit placeholder or stub states and never imply a real framework object exists when it does not
- threat_id: T-03-04
  category: denial-of-service
  component: post-receiver-blocker
  disposition: mitigate
  mitigation_plan: map every newly reached seam to an exact blocker string so the runtime does not collapse into generic execution failure
</stride_register>
</threat_model>

<tasks>
<task type="auto">
  <name>Task 1: Tighten the next lifecycle runtime-context seam after receiver propagation</name>
  <files>src/apk_dex_bridge.cpp, include/wfa/apk_dex_bridge.hpp</files>
  <read_first>src/apk_dex_bridge.cpp, include/wfa/apk_dex_bridge.hpp, .planning/phases/03-runtime-context-bridge/03-RESEARCH.md</read_first>
  <action>Advance the minimal interpreter one small step farther than receiver propagation by modeling the smallest needed lifecycle parameter, framework-owned callee placeholder, or pending-result continuity seam. Keep any placeholder or framework-stub state explicit and fail closed with an exact blocker when the runtime still cannot continue into a real managed context.</action>
  <verify>cmake --build build</verify>
  <acceptance_criteria>
    - a new exact post-receiver blocker replaces generic framework collapse
    - any placeholder or stub state is labeled explicitly
    - no full ART or ActivityThread claim is introduced
  </acceptance_criteria>
  <done>The post-receiver seam is now concrete enough to hand the next phase one exact runtime-context blocker.</done>
</task>

<task type="auto">
  <name>Task 2: Surface the post-receiver seam and exact blocker through first-app-start reporting</name>
  <files>src/apk_native_launch.cpp, tests/test_main.cpp</files>
  <read_first>src/apk_native_launch.cpp, tests/test_main.cpp, .planning/phases/03-runtime-context-bridge/03-PATTERNS.md</read_first>
  <action>Thread the new post-receiver seam into `first_android_app_start`, including exact lifecycle-parameter or framework-callee state, the current bytecode backend, and the exact next blocker. Update regression coverage so the synthetic seam cannot silently fall back to `needs-real-activitythread-context` or another broad label when a smaller blocker is now known.</action>
  <verify>cmake --build build && ctest --test-dir build --output-on-failure</verify>
  <acceptance_criteria>
    - `first_android_app_start` exposes the post-receiver seam explicitly
    - tests pin the new blocker string or explicit placeholder/stub state
    - existing real keyboard activity lookup assertions remain green
  </acceptance_criteria>
  <done>The report now tells later phases exactly what comes after receiver propagation.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `cmake --build build`
- [ ] `ctest --test-dir build --output-on-failure`
- [ ] The first-app-start report names a post-receiver runtime-context seam more precise than `needs-real-activitythread-context`
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The remaining blocker is more actionable than the current broad ActivityThread boundary
</success_criteria>

<output>
After completion, create `.planning/phases/03-runtime-context-bridge/03-02-SUMMARY.md`
</output>
