---
phase: 08-native-app-start-bridge
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/native_execute_stub.cpp
  - include/wfa/native_execute_stub.hpp
  - src/apk_native_launch.cpp
  - tests/test_main.cpp
autonomous: true
requirements: [JNI-05]
user_setup: []
must_haves:
  truths:
    - "Linuxoid no longer stops at a generic missing `ANativeActivity_onCreate` seam for `libjni_latinime.so`."
    - "The real keyboard APK path moves into a Linuxoid-owned app-start strategy or a smaller exact post-`JNI_OnLoad` seam."
  artifacts:
    - path: "src/native_execute_stub.cpp"
      provides: "Linuxoid-owned app-start bridge selection for JNI-shaped primary libraries"
    - path: "src/apk_native_launch.cpp"
      provides: "Operator-facing launch truth for the new app-start seam"
  key_links:
    - from: "src/native_execute_stub.cpp"
      to: "src/apk_native_launch.cpp"
      via: "native execute report and launch status"
      pattern: "native_activity_entrypoint_missing"
---

<objective>
Close the first post-`JNI_OnLoad` app-start gap for the real keyboard APK.

Purpose: Move Linuxoid from a generic JNI-shaped missing-entrypoint boundary into a Linuxoid-owned app-start strategy or a smaller exact blocker.
Output: A smaller, exact app-start seam than the current `native_activity_entrypoint_missing` boundary.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-native-app-start-bridge/08-RESEARCH.md
@.planning/phases/08-native-app-start-bridge/08-PATTERNS.md
@src/native_execute_stub.cpp
@include/wfa/native_execute_stub.hpp
@src/apk_native_launch.cpp
@tests/test_main.cpp
</context>

<threat_model>
<trust_boundaries>
- Untrusted staged `.so` payloads extracted from `/home/astra/Downloads/keyboard-0.1.28.apk`
- Linuxoid-owned app-start bridging reflected back into operator-facing JSON
</trust_boundaries>
<stride_register>
- threat_id: T-08-01
  category: tampering
  component: native-app-start-bridge
  disposition: mitigate
  mitigation_plan: keep any app-start bridge explicit, deterministic, and limited to the minimum surface needed to move the real keyboard seam
</stride_register>
</threat_model>

<tasks>
<task type="auto">
  <name>Task 1: Move beyond generic missing-entrypoint failure for `libjni_latinime.so`</name>
  <files>src/native_execute_stub.cpp, include/wfa/native_execute_stub.hpp, src/apk_native_launch.cpp, tests/test_main.cpp</files>
  <read_first>src/native_execute_stub.cpp, src/apk_native_launch.cpp, .planning/phases/08-native-app-start-bridge/08-RESEARCH.md</read_first>
  <action>Extend or tighten the native execute path so a JNI-shaped primary library can advance into a Linuxoid-owned app-start strategy or a smaller exact post-`JNI_OnLoad` seam. Preserve deterministic reporting and do not claim app start success if Linuxoid still lacks the required bridge.</action>
  <verify>TMPDIR=/home/astra/codex/wine-for-android/.tmp cmake --build build</verify>
  <acceptance_criteria>
    - the real keyboard APK seam is smaller than the current `native_activity_entrypoint_missing` blocker
    - `native_execute.*` reporting stays deterministic
    - fixture/native app-start coverage stays offline and green
  </acceptance_criteria>
  <done>Linuxoid reaches a smaller post-`JNI_OnLoad` seam for the real keyboard APK.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `TMPDIR=/home/astra/codex/wine-for-android/.tmp cmake --build build`
- [ ] `TMPDIR=/home/astra/codex/wine-for-android/.tmp ctest --test-dir build --output-on-failure`
- [ ] Real or representative launch JSON shows a smaller app-start blocker than the current `native_activity_entrypoint_missing` failure
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Linuxoid narrows the real keyboard APK post-`JNI_OnLoad` seam without pretending full native or managed success
</success_criteria>

<output>
After completion, create `.planning/phases/08-native-app-start-bridge/08-01-SUMMARY.md`
</output>
