---
phase: 08-native-app-start-bridge
plan: "03"
type: execute
wave: 3
depends_on: ["02"]
files_modified:
  - src/apk_native_launch.cpp
  - src/apk_self_healing_watchdog.cpp
  - tests/test_main.cpp
  - README.md
  - CHANGELOG.md
  - docs/phased-build-plan.md
  - docs/self-healing-runtime-skeleton.md
  - docs/steps.md
  - src/project_status.cpp
autonomous: true
requirements: [VER-05]
user_setup: []
must_haves:
  truths:
    - "Repo docs, first-app-start JSON, and watchdog output agree on the narrowed post-`JNI_OnLoad` blocker."
    - "The later managed blocker remains explicit once the upstream native app-start seam moves."
  artifacts:
    - path: "src/apk_native_launch.cpp"
      provides: "First-app-start and launch blocker propagation"
    - path: "src/apk_self_healing_watchdog.cpp"
      provides: "Self-Healing Android Device recovery wording rooted in the earliest blocker"
  key_links:
    - from: "src/apk_native_launch.cpp"
      to: "src/apk_self_healing_watchdog.cpp"
      via: "primary blocker and recommended next action"
      pattern: "primary_blocker_reason"
---

<objective>
Lock the new post-`JNI_OnLoad` seam into first-app-start, recovery, and repo truth.

Purpose: Make the narrower real keyboard APK blocker durable and easy to reason about for the next execution slice.
Output: Stable JSON, docs, and regression coverage that all point at the same remaining blocker.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-native-app-start-bridge/08-RESEARCH.md
@.planning/phases/08-native-app-start-bridge/08-VALIDATION.md
@src/apk_native_launch.cpp
@src/apk_self_healing_watchdog.cpp
@tests/test_main.cpp
@README.md
@CHANGELOG.md
@docs/phased-build-plan.md
@docs/self-healing-runtime-skeleton.md
@docs/steps.md
@src/project_status.cpp
</context>

<threat_model>
<trust_boundaries>
- Narrowed real-APK blocker crossing from runtime code into operator docs, status, and watchdog output
</trust_boundaries>
<stride_register>
- threat_id: T-08-03
  category: repudiation
  component: runtime-truth-and-docs-consistency
  disposition: mitigate
  mitigation_plan: pin the new blocker in tests, first-app-start JSON, watchdog gating, and repo truth so later slices cannot silently drift back to stale blocker language
</stride_register>
</threat_model>

<tasks>
<task type="auto">
  <name>Task 1: Keep first-app-start, recovery, and docs aligned on the new app-start blocker</name>
  <files>src/apk_native_launch.cpp, src/apk_self_healing_watchdog.cpp, tests/test_main.cpp, README.md, CHANGELOG.md, docs/phased-build-plan.md, docs/self-healing-runtime-skeleton.md, docs/steps.md, src/project_status.cpp</files>
  <read_first>src/apk_native_launch.cpp, src/apk_self_healing_watchdog.cpp, tests/test_main.cpp, .planning/phases/08-native-app-start-bridge/08-VALIDATION.md</read_first>
  <action>Update operator surfaces and regression coverage so the narrowed app-start blocker becomes the stable repo truth. Preserve the downstream managed blocker as the next seam instead of letting the repo drift back to older native-entry wording.</action>
  <verify>TMPDIR=/home/astra/codex/wine-for-android/.tmp cmake --build build && TMPDIR=/home/astra/codex/wine-for-android/.tmp ctest --test-dir build --output-on-failure</verify>
  <acceptance_criteria>
    - docs/status/reports point at the same current blocker
    - watchdog recovery wording stays rooted in the earliest app-start seam
    - tests fail if first-app-start regresses to stale blocker language
  </acceptance_criteria>
  <done>The next execution slice starts from one stable, honest post-`JNI_OnLoad` blocker story.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `TMPDIR=/home/astra/codex/wine-for-android/.tmp cmake --build build`
- [ ] `TMPDIR=/home/astra/codex/wine-for-android/.tmp ctest --test-dir build --output-on-failure`
- [ ] Real or representative first-app-start output, watchdog output, and docs agree on the same narrowed blocker and next managed seam
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The narrowed post-`JNI_OnLoad` seam becomes stable repo truth
</success_criteria>

<output>
After completion, create `.planning/phases/08-native-app-start-bridge/08-03-SUMMARY.md`
</output>
