---
phase: 08-native-app-start-bridge
plan: "02"
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/native_execute_stub.cpp
  - include/wfa/native_execute_stub.hpp
  - src/apk_native_launch.cpp
  - include/wfa/apk_native_launch.hpp
  - tests/test_main.cpp
autonomous: true
requirements: [JNI-06]
user_setup: []
must_haves:
  truths:
    - "Once the JNI-shaped primary library advances past the current boundary, Linuxoid reports the first true registration or startup seam exactly instead of collapsing into generic entrypoint failure."
    - "JNI, app-start bridge, and later managed-start states stay distinct in launch JSON."
  artifacts:
    - path: "src/native_execute_stub.cpp"
      provides: "Exact post-`JNI_OnLoad` registration or startup seam"
    - path: "src/apk_native_launch.cpp"
      provides: "Operator-facing launch and first-app-start truth for the new seam"
  key_links:
    - from: "src/native_execute_stub.cpp"
      to: "src/apk_native_launch.cpp"
      via: "native loading state -> first-app-start blocker propagation"
      pattern: "native_activity_entrypoint_missing"
---

<objective>
Expose the first true post-`JNI_OnLoad` startup boundary for the keyboard APK.

Purpose: Move Linuxoid from generic missing-entrypoint failure into an exact JNI registration, app-start bridge, or managed-bootstrap seam.
Output: Deterministic post-`JNI_OnLoad` truth through `launch-apk` and `--first-app-start-proof`.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-native-app-start-bridge/08-RESEARCH.md
@.planning/phases/08-native-app-start-bridge/08-PATTERNS.md
@src/native_execute_stub.cpp
@include/wfa/native_execute_stub.hpp
@src/apk_native_launch.cpp
@include/wfa/apk_native_launch.hpp
@tests/test_main.cpp
</context>

<threat_model>
<trust_boundaries>
- Post-`JNI_OnLoad` native startup and registration behavior crossing into user-visible diagnostics
</trust_boundaries>
<stride_register>
- threat_id: T-08-02
  category: spoofing
  component: native-app-start-diagnostics
  disposition: mitigate
  mitigation_plan: separate `JNI_OnLoad`, app-start bridge, registration, and later managed-start states explicitly so Linuxoid cannot accidentally overclaim startup progress
</stride_register>
</threat_model>

<tasks>
<task type="auto">
  <name>Task 1: Surface the first post-`JNI_OnLoad` startup boundary exactly</name>
  <files>src/native_execute_stub.cpp, include/wfa/native_execute_stub.hpp, src/apk_native_launch.cpp, include/wfa/apk_native_launch.hpp</files>
  <read_first>src/native_execute_stub.cpp, src/apk_native_launch.cpp, include/wfa/apk_native_launch.hpp, .planning/phases/08-native-app-start-bridge/08-PATTERNS.md</read_first>
  <action>Extend the current native execute path so once the primary library loads and `JNI_OnLoad` succeeds, Linuxoid exposes the next exact startup seam: registration, Linuxoid-owned bridge dispatch, or a specific managed bootstrap dependency. Keep the JSON stable and preserve the downstream managed blocker as a later seam, not a replacement for native truth.</action>
  <verify>TMPDIR=/home/astra/codex/wine-for-android/.tmp cmake --build build && TMPDIR=/home/astra/codex/wine-for-android/.tmp ctest --test-dir build --output-on-failure</verify>
  <acceptance_criteria>
    - post-`JNI_OnLoad` startup states stay distinct and machine-readable
    - `launch-apk --first-app-start-proof` reports the first true app-start seam once the library loads
    - deterministic offline regression coverage pins the new boundary
  </acceptance_criteria>
  <done>Linuxoid exposes the first real post-`JNI_OnLoad` startup boundary for the keyboard APK path.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `TMPDIR=/home/astra/codex/wine-for-android/.tmp cmake --build build`
- [ ] `TMPDIR=/home/astra/codex/wine-for-android/.tmp ctest --test-dir build --output-on-failure`
- [ ] Launch JSON distinguishes load success, `JNI_OnLoad`, app-start bridge, registration, and later managed seams
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Linuxoid reaches the first true app-start seam for the real keyboard APK path
</success_criteria>

<output>
After completion, create `.planning/phases/08-native-app-start-bridge/08-02-SUMMARY.md`
</output>
