---
phase: 07-native-libc-and-entry-bridge
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/native_execute_stub.cpp
  - include/wfa/native_execute_stub.hpp
  - src/android_bionic_libc_compat.cpp
  - src/android_passthrough_compat.cpp
  - CMakeLists.txt
  - tests/test_main.cpp
autonomous: true
requirements: [JNI-03]
user_setup: []
must_haves:
  truths:
    - "Linuxoid narrows the real keyboard APK blocker from generic native-entry failure to an exact Android-libc symbol or a smaller post-load seam."
    - "The real entry library `libjni_latinime.so` is treated as the authoritative target instead of older helper-library failures."
  artifacts:
    - path: "src/native_execute_stub.cpp"
      provides: "Android-libc/native-entry compatibility and exact entry-library load truth"
    - path: "src/android_bionic_libc_compat.cpp"
      provides: "Minimal host-side shims for the real entry-library symbol gap"
  key_links:
    - from: "src/apk_native_launch.cpp"
      to: "src/native_execute_stub.cpp"
      via: "native execute report and launch status"
      pattern: "native_loading_detail"
---

<objective>
Close the first Android-libc symbol gap for the real keyboard APK entry library.

Purpose: Move Linuxoid past the current `libjni_latinime.so` / `__strchr_chk` blocker on the direct launch path.
Output: A smaller, exact native seam than the current unresolved-symbol failure.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-native-libc-and-entry-bridge/07-RESEARCH.md
@.planning/phases/07-native-libc-and-entry-bridge/07-PATTERNS.md
@src/native_execute_stub.cpp
@include/wfa/native_execute_stub.hpp
@src/android_bionic_libc_compat.cpp
@src/android_passthrough_compat.cpp
@tests/test_main.cpp
</context>

<threat_model>
<trust_boundaries>
- Untrusted staged `.so` payloads extracted from `/home/astra/Downloads/keyboard-0.1.28.apk`
- Host-side shim/preload behavior reflected back into operator-facing JSON
</trust_boundaries>
<stride_register>
- threat_id: T-07-01
  category: tampering
  component: native-libc-compatibility-layer
  disposition: mitigate
  mitigation_plan: keep compatibility shims explicit, deterministic, and limited to the minimum surface needed to move the real entry-library seam
</stride_register>
</threat_model>

<tasks>
<task type="auto">
  <name>Task 1: Move `libjni_latinime.so` past the first unresolved Android-libc symbol seam</name>
  <files>src/native_execute_stub.cpp, include/wfa/native_execute_stub.hpp, src/android_bionic_libc_compat.cpp, src/android_passthrough_compat.cpp, CMakeLists.txt</files>
  <read_first>src/native_execute_stub.cpp, src/android_bionic_libc_compat.cpp, src/android_passthrough_compat.cpp, .planning/phases/07-native-libc-and-entry-bridge/07-RESEARCH.md</read_first>
  <action>Extend or tighten the existing Android-compat preload surface so the real entry library either loads past `__strchr_chk` or fails at a smaller exact symbol seam. Preserve deterministic reporting and do not claim JNI readiness if the library still does not load.</action>
  <verify>TMPDIR=/home/astra/codex/wine-for-android/.tmp cmake --build build</verify>
  <acceptance_criteria>
    - the real keyboard APK seam is smaller than the current `undefined symbol: __strchr_chk` blocker, or that symbol remains the exact reported blocker without ambiguity
    - `native_execute.android_compat_*` reporting stays deterministic
    - fixture/native compatibility coverage stays offline and green
  </acceptance_criteria>
  <done>Linuxoid reaches a smaller native-load seam for the real entry library.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `TMPDIR=/home/astra/codex/wine-for-android/.tmp cmake --build build`
- [ ] `TMPDIR=/home/astra/codex/wine-for-android/.tmp ctest --test-dir build --output-on-failure`
- [ ] Real or representative launch JSON shows a smaller entry-library blocker than the current `__strchr_chk` failure
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Linuxoid narrows the real keyboard APK entry-library seam without pretending full native success
</success_criteria>

<output>
After completion, create `.planning/phases/07-native-libc-and-entry-bridge/07-01-SUMMARY.md`
</output>
