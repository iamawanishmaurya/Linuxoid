---
phase: 04-jni-and-native-loading
plan: "02"
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/apk_native_launch.cpp
  - include/wfa/apk_native_launch.hpp
  - src/runtime_health.cpp
  - tests/test_main.cpp
  - README.md
  - CHANGELOG.md
  - docs/phased-build-plan.md
  - docs/self-healing-runtime-skeleton.md
  - docs/steps.md
autonomous: true
requirements: [JNI-02]
user_setup: []
must_haves:
  truths:
    - "Linuxoid reports the exact JNI/native blocker for the verification app through operator-facing launch and first-app-start surfaces."
    - "Self-Healing Android Device wording stays honest about what native/JNI progress exists and what blocker remains."
  artifacts:
    - path: "src/apk_native_launch.cpp"
      provides: "Native/JNI blocker propagation into operator-facing proof surfaces"
    - path: "src/runtime_health.cpp"
      provides: "Higher-level native/JNI health wording that matches the launch facts"
  key_links:
    - from: "src/apk_native_launch.cpp"
      to: "src/runtime_health.cpp"
      via: "native/JNI blocker classification and recovery guidance"
      pattern: "inspect_native_launch_diagnostics"
---

<objective>
Add exact JNI/native failure reporting for the verification app and preserve it through Linuxoid's operator-facing proof surfaces.

Purpose: Once Phase 4 narrows native-load truth, Linuxoid should carry that truth through first-app-start and recovery output without pretending native/JNI success or masking the downstream managed seam.
Output: Exact JNI/native blocker fields, consistent recovery wording, and updated docs/status around the verification app's native boundary.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-jni-and-native-loading/04-RESEARCH.md
@.planning/phases/04-jni-and-native-loading/04-PATTERNS.md
@src/apk_native_launch.cpp
@include/wfa/apk_native_launch.hpp
@src/runtime_health.cpp
@tests/test_main.cpp
@README.md
</context>

<threat_model>
<trust_boundaries>
- Native/JNI blocker facts crossing from low-level launcher code into higher-level proof JSON and recovery wording
- User-visible docs/status that can accidentally overclaim progress
</trust_boundaries>
<stride_register>
- threat_id: T-04-03
  category: tampering
  component: blocker-propagation
  disposition: mitigate
  mitigation_plan: propagate exact low-level native/JNI reasons upward instead of remapping them to vague blocked states
- threat_id: T-04-04
  category: information_disclosure
  component: user-visible-status
  disposition: mitigate
  mitigation_plan: keep docs and status precise, deterministic, and aligned with the actual launch facts; avoid overstating JNI or framework readiness
</stride_register>
</threat_model>

<tasks>
<task type="auto">
  <name>Task 1: Thread exact native/JNI blocker truth through first-app-start and health surfaces</name>
  <files>src/apk_native_launch.cpp, include/wfa/apk_native_launch.hpp, src/runtime_health.cpp</files>
  <read_first>src/apk_native_launch.cpp, include/wfa/apk_native_launch.hpp, src/runtime_health.cpp, .planning/phases/04-jni-and-native-loading/04-RESEARCH.md</read_first>
  <action>Use the narrowed native/JNI facts from Plan 04-01 to keep `launch-apk`, `--first-app-start-proof`, and higher-level health/recovery fields aligned. The upstream native seam should stay explicit when it blocks managed execution, while the downstream stubbed `Activity.onCreate(Bundle)` seam remains visible as the next later blocker once native loading is solved.</action>
  <verify>TMPDIR=/home/astra/codex/wine-for-android/.tmp cmake --build build</verify>
  <acceptance_criteria>
    - operator-facing JSON reports the exact native/JNI blocker instead of collapsing to generic blocked state
    - `src/runtime_health.cpp` and recovery wording match the same low-level native/JNI truth
    - the existing managed Bundle-boundary seam remains documented as downstream, not solved
  </acceptance_criteria>
  <done>Linuxoid surfaces one coherent native/JNI blocker story across launch, proof, and health output.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the verification-app native/JNI seam into tests and repo truth</name>
  <files>tests/test_main.cpp, README.md, CHANGELOG.md, docs/phased-build-plan.md, docs/self-healing-runtime-skeleton.md, docs/steps.md</files>
  <read_first>tests/test_main.cpp, README.md, docs/phased-build-plan.md, .planning/phases/04-jni-and-native-loading/04-PATTERNS.md</read_first>
  <action>Add or update regression coverage so native/JNI blocker propagation stays exact, then refresh README/docs/status with the concrete Phase 4 checkpoint and next blocker. Keep the docs honest that this is still a native/JNI seam, not full app success, and preserve the downstream managed-runtime seam language.</action>
  <verify>TMPDIR=/home/astra/codex/wine-for-android/.tmp cmake --build build && TMPDIR=/home/astra/codex/wine-for-android/.tmp ctest --test-dir build --output-on-failure</verify>
  <acceptance_criteria>
    - regression coverage fails if native/JNI blocker reporting collapses back into ambiguity
    - README/docs/status name the exact native/JNI blocker and the next blocker after it
    - repo truth remains execution-first and does not overclaim JNI or framework progress
  </acceptance_criteria>
  <done>The verification-app native/JNI seam is durable in tests, CLI output, and docs.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `TMPDIR=/home/astra/codex/wine-for-android/.tmp cmake --build build`
- [ ] `TMPDIR=/home/astra/codex/wine-for-android/.tmp ctest --test-dir build --output-on-failure`
- [ ] Launch and first-app-start output agree on the exact upstream native/JNI blocker for the verification app
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Linuxoid exposes one exact native/JNI blocker story for the real verification app
</success_criteria>

<output>
After completion, create `.planning/phases/04-jni-and-native-loading/04-02-SUMMARY.md`
</output>
