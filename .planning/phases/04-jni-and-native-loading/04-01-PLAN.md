---
phase: 04-jni-and-native-loading
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/apk_native_launch.cpp
  - include/wfa/apk_native_launch.hpp
  - src/native_execute_stub.cpp
  - include/wfa/native_execute_stub.hpp
  - tests/test_main.cpp
autonomous: true
requirements: [JNI-01]
user_setup: []
must_haves:
  truths:
    - "Linuxoid stages the real keyboard APK `x86_64` libraries through the normal `launch-apk` path and reports native-load attempts deterministically."
    - "The real keyboard APK no longer stops at only a generic `libraries_failed_to_load` reason without deeper native-load evidence."
  artifacts:
    - path: "src/native_execute_stub.cpp"
      provides: "Per-library `dlopen` and `JNI_OnLoad` attempt truth"
    - path: "src/apk_native_launch.cpp"
      provides: "Real APK launch JSON that preserves exact native-load state"
  key_links:
    - from: "src/apk_native_launch.cpp"
      to: "src/native_execute_stub.cpp"
      via: "native_execute report and real APK launch status"
      pattern: "libraries_failed_to_load"
---

<objective>
Bring real `x86_64` native-library staging and load-attempt truth into the keyboard APK launch path.

Purpose: Phase 4 should replace the generic real-APK `libraries_failed_to_load` seam with a smaller, exact native-load blocker.
Output: Deterministic per-library load-attempt facts and exact native/JNI status through `launch-apk`.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-runtime-context-bridge/03-03-SUMMARY.md
@.planning/phases/04-jni-and-native-loading/04-RESEARCH.md
@.planning/phases/04-jni-and-native-loading/04-PATTERNS.md
@src/apk_native_launch.cpp
@include/wfa/apk_native_launch.hpp
@src/native_execute_stub.cpp
@include/wfa/native_execute_stub.hpp
@tests/test_main.cpp
</context>

<threat_model>
<trust_boundaries>
- Untrusted native `.so` payloads extracted from `/home/astra/Downloads/keyboard-0.1.28.apk`
- Native loader errors and captured debug output reflected back into machine-readable JSON
</trust_boundaries>
<stride_register>
- threat_id: T-04-01
  category: tampering
  component: native-library-selection-and-reporting
  disposition: mitigate
  mitigation_plan: preserve deterministic candidate ordering, selected-ABI scoping, and exact per-library status without letting one failing library overwrite the entire load trace
- threat_id: T-04-02
  category: denial-of-service
  component: native-loader-attempt-loop
  disposition: mitigate
  mitigation_plan: attempt only the staged host-ABI candidates in deterministic order, capture exact `dlopen`/JNI status, and fail closed with an exact blocker instead of retry loops or ambiguous collapse
</stride_register>
</threat_model>

<tasks>
<task type="auto">
  <name>Task 1: Expose deterministic per-library load-attempt state for the real APK path</name>
  <files>src/native_execute_stub.cpp, include/wfa/native_execute_stub.hpp, src/apk_native_launch.cpp</files>
  <read_first>src/native_execute_stub.cpp, include/wfa/native_execute_stub.hpp, src/apk_native_launch.cpp, .planning/phases/04-jni-and-native-loading/04-RESEARCH.md</read_first>
  <action>Extend the native execute report so `launch-apk` can expose deterministic candidate order, attempted library paths, and exact load/JNI status for each staged host-ABI candidate. Preserve the current fixture-native success path while making the real keyboard APK blocker smaller than generic `libraries_failed_to_load`.</action>
  <verify>TMPDIR=/home/astra/codex/wine-for-android/.tmp cmake --build build</verify>
  <acceptance_criteria>
    - `src/native_execute_stub.cpp` records deterministic per-library native load outcomes instead of only the final exit reason
    - `src/apk_native_launch.cpp` surfaces those outcomes in launch JSON without hiding `selected_abi` or the staged library list
    - existing fixture-native success behavior remains intact
  </acceptance_criteria>
  <done>The real keyboard APK native-load seam is smaller and machine-readable.</done>
</task>

<task type="auto">
  <name>Task 2: Pin the real and fixture native-load seams in regression coverage</name>
  <files>tests/test_main.cpp, src/apk_native_launch.cpp</files>
  <read_first>tests/test_main.cpp, src/apk_native_launch.cpp, .planning/phases/04-jni-and-native-loading/04-PATTERNS.md</read_first>
  <action>Add regression coverage that keeps fixture-native success green while asserting the real keyboard-like negative path reports exact per-library native-load or JNI state instead of only a generic failure. Keep tests deterministic and offline by using fixture or synthetic negative libraries where needed, and leave the real keyboard APK as a manual smoke target.</action>
  <verify>TMPDIR=/home/astra/codex/wine-for-android/.tmp cmake --build build && TMPDIR=/home/astra/codex/wine-for-android/.tmp ctest --test-dir build --output-on-failure</verify>
  <acceptance_criteria>
    - `tests/test_main.cpp` contains deterministic assertions for exact native-load/JNI blocker reporting
    - the existing fixture native-launch success path still passes
    - the report surface distinguishes load failure from later JNI or entrypoint seams
  </acceptance_criteria>
  <done>The native-load seam is pinned in tests and cannot silently regress to generic failure output.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `TMPDIR=/home/astra/codex/wine-for-android/.tmp cmake --build build`
- [ ] `TMPDIR=/home/astra/codex/wine-for-android/.tmp ctest --test-dir build --output-on-failure`
- [ ] Real or representative launch JSON reports a smaller native-load seam than generic `libraries_failed_to_load`
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The keyboard APK native-load seam is narrower and more actionable than the current generic failure
</success_criteria>

<output>
After completion, create `.planning/phases/04-jni-and-native-loading/04-01-SUMMARY.md`
</output>
