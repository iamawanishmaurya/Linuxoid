---
phase: 11-managed-activity-dispatch-bridge
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/native_execute_stub.cpp
  - include/wfa/native_execute_stub.hpp
  - src/apk_native_launch.cpp
  - include/wfa/apk_native_launch.hpp
  - tests/test_main.cpp
autonomous: true
requirements: [JNI-11]
user_setup: []
must_haves:
  truths:
    - "Linuxoid no longer stops at the generic `managed_activity_dispatch_required` seam for `libjni_latinime.so`."
    - "The real keyboard APK path moves into a smaller exact managed activity dispatch seam."
  artifacts:
    - path: "src/native_execute_stub.cpp"
      provides: "Linuxoid-owned managed activity dispatch bridge attempt beyond the current seam"
    - path: "src/apk_native_launch.cpp"
      provides: "Operator-facing launch truth for the new managed dispatch seam"
  key_links:
    - from: "src/native_execute_stub.cpp"
      to: "src/apk_native_launch.cpp"
      via: "managed dispatch report and launch status"
      pattern: "managed_activity_dispatch_required"
---

<objective>
Close the first managed activity dispatch gap for the real keyboard APK.

Purpose: Move Linuxoid from a completed JNI registration seam into a real managed activity dispatch attempt.
Output: A smaller, exact seam than the current `managed_activity_dispatch_required` boundary.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-managed-activity-dispatch-bridge/11-RESEARCH.md
@.planning/phases/11-managed-activity-dispatch-bridge/11-PATTERNS.md
@src/native_execute_stub.cpp
@include/wfa/native_execute_stub.hpp
@src/apk_native_launch.cpp
@include/wfa/apk_native_launch.hpp
@tests/test_main.cpp
</context>

<threat_model>
<trust_boundaries>
- Post-registration managed activity dispatch behavior crossing into operator-facing JSON
</trust_boundaries>
<stride_register>
- threat_id: T-11-01
  category: spoofing
  component: managed-activity-dispatch
  disposition: mitigate
  mitigation_plan: keep any managed activity dispatch attempt explicit, deterministic, and limited to the minimum surface needed to move the real keyboard seam
</stride_register>
</threat_model>

<tasks>
<task type="auto">
  <name>Task 1: Move beyond generic managed-dispatch failure for `libjni_latinime.so`</name>
  <files>src/native_execute_stub.cpp, include/wfa/native_execute_stub.hpp, src/apk_native_launch.cpp, include/wfa/apk_native_launch.hpp, tests/test_main.cpp</files>
  <read_first>src/native_execute_stub.cpp, src/apk_native_launch.cpp, .planning/phases/11-managed-activity-dispatch-bridge/11-RESEARCH.md</read_first>
  <action>Extend or tighten the native execute and first-app-start path so a JNI-registered primary library can advance beyond the current managed-dispatch-required state into a Linuxoid-owned managed activity dispatch attempt or a smaller exact managed seam. Preserve deterministic reporting and do not claim app start success if Linuxoid still lacks the required runtime-context bridge.</action>
  <verify>TMPDIR=/home/astra/codex/wine-for-android/.tmp cmake --build build</verify>
  <acceptance_criteria>
    - the real keyboard APK seam is smaller than the current `managed_activity_dispatch_required` blocker
    - managed dispatch reporting stays deterministic
    - fixture/managed-dispatch coverage stays offline and green
  </acceptance_criteria>
  <done>Linuxoid reaches a smaller managed dispatch seam for the real keyboard APK.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `TMPDIR=/home/astra/codex/wine-for-android/.tmp cmake --build build`
- [ ] `TMPDIR=/home/astra/codex/wine-for-android/.tmp ctest --test-dir build --output-on-failure`
- [ ] Real or representative launch JSON shows a smaller app-start blocker than the current `managed_activity_dispatch_required` failure
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Linuxoid narrows the real keyboard APK managed activity dispatch seam without pretending full managed success
</success_criteria>

<output>
After completion, create `.planning/phases/11-managed-activity-dispatch-bridge/11-01-SUMMARY.md`
</output>

