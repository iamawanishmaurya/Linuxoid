---
phase: 12-managed-runtime-context-binding
plan: "02"
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/native_execute_stub.cpp
  - include/wfa/native_execute_stub.hpp
  - src/apk_native_launch.cpp
  - include/wfa/apk_native_launch.hpp
  - src/apk_dex_bridge.cpp
  - include/wfa/apk_dex_bridge.hpp
  - tests/test_main.cpp
autonomous: true
requirements: [JNI-14]
user_setup: []
must_haves:
  truths:
    - "Once the managed runtime-context bridge advances past the current seam, Linuxoid reports the first true post-binding lifecycle or framework boundary exactly."
    - "Managed activity dispatch, runtime-context binding, lifecycle dispatch, and later managed-start states stay distinct in launch JSON."
  artifacts:
    - path: "src/native_execute_stub.cpp"
      provides: "Exact post-binding seam"
    - path: "src/apk_native_launch.cpp"
      provides: "Operator-facing launch and first-app-start truth for the new seam"
  key_links:
    - from: "src/native_execute_stub.cpp"
      to: "src/apk_native_launch.cpp"
      via: "managed runtime-context state -> first-app-start blocker propagation"
      pattern: "managed_runtime_context_required"
---

<objective>
Expose the first true post-binding startup boundary for the keyboard APK.

Purpose: Move Linuxoid from managed-runtime-context-required failure into an exact lifecycle or framework seam.
Output: Deterministic post-binding truth through `launch-apk` and `--first-app-start-proof`.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-managed-runtime-context-binding/12-RESEARCH.md
@.planning/phases/12-managed-runtime-context-binding/12-PATTERNS.md
@src/native_execute_stub.cpp
@include/wfa/native_execute_stub.hpp
@src/apk_native_launch.cpp
@include/wfa/apk_native_launch.hpp
@src/apk_dex_bridge.cpp
@include/wfa/apk_dex_bridge.hpp
@tests/test_main.cpp
</context>

<threat_model>
<trust_boundaries>
- Post-binding managed bootstrap behavior crossing into user-visible diagnostics
</trust_boundaries>
<stride_register>
- threat_id: T-12-02
  category: spoofing
  component: managed-runtime-diagnostics
  disposition: mitigate
  mitigation_plan: separate managed activity dispatch selection, managed runtime-context binding, lifecycle dispatch, and later framework bootstrap states explicitly so Linuxoid cannot accidentally overclaim startup progress
</stride_register>
</threat_model>

<tasks>
<task type="auto">
  <name>Task 1: Surface the first post-binding startup boundary exactly</name>
  <files>src/native_execute_stub.cpp, include/wfa/native_execute_stub.hpp, src/apk_native_launch.cpp, include/wfa/apk_native_launch.hpp, src/apk_dex_bridge.cpp, include/wfa/apk_dex_bridge.hpp</files>
  <read_first>src/native_execute_stub.cpp, src/apk_native_launch.cpp, include/wfa/apk_native_launch.hpp, src/apk_dex_bridge.cpp, .planning/phases/12-managed-runtime-context-binding/12-PATTERNS.md</read_first>
  <action>Extend the current native execute and first-app-start path so once the primary library loads, `JNI_OnLoad` succeeds, JNI registration completes, Linuxoid dispatches the managed activity bridge, and Linuxoid binds a managed runtime-context attempt, Linuxoid exposes the next exact startup seam: lifecycle dispatch dependency, framework boundary, or a specific managed bootstrap seam. Keep the JSON stable and preserve the downstream managed blocker as a later seam, not a replacement for binding truth.</action>
  <verify>TMPDIR=/home/astra/codex/wine-for-android/.tmp cmake --build build && TMPDIR=/home/astra/codex/wine-for-android/.tmp ctest --test-dir build --output-on-failure</verify>
  <acceptance_criteria>
    - post-binding startup states stay distinct and machine-readable
    - `launch-apk --first-app-start-proof` reports the first true post-binding seam once managed runtime-context binding starts
    - deterministic offline regression coverage pins the new boundary
  </acceptance_criteria>
  <done>Linuxoid exposes the first real post-binding startup boundary for the keyboard APK path.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `TMPDIR=/home/astra/codex/wine-for-android/.tmp cmake --build build`
- [ ] `TMPDIR=/home/astra/codex/wine-for-android/.tmp ctest --test-dir build --output-on-failure`
- [ ] Launch JSON distinguishes managed activity dispatch, managed runtime-context binding, lifecycle dispatch, and later managed seams
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Linuxoid reaches the first true post-binding seam for the real keyboard APK path
</success_criteria>

<output>
After completion, create `.planning/phases/12-managed-runtime-context-binding/12-02-SUMMARY.md`
</output>
