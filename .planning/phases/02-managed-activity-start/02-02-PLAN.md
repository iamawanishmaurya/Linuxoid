---
phase: 02-managed-activity-start
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/apk_dex_bridge.cpp
  - include/wfa/apk_dex_bridge.hpp
  - src/apk_native_launch.cpp
  - tests/test_main.cpp
autonomous: true
requirements: [DEX-01, DEX-03]
user_setup: []
must_haves:
  truths:
    - "Linuxoid can report whether the real keyboard activity's lifecycle entrypoint was resolved, unresolved, or blocked at an exact DEX boundary."
    - "The first-app-start proof preserves honest blockers such as missing method, missing code item, or unsupported opcode for the real activity path."
  artifacts:
    - path: "src/apk_dex_bridge.cpp"
      provides: "Real activity lifecycle method lookup and blocker reporting"
    - path: "tests/test_main.cpp"
      provides: "Regression coverage for real activity DEX entrypoint reporting"
  key_links:
    - from: "src/apk_dex_bridge.cpp"
      to: "src/apk_native_launch.cpp"
      via: "execution probe lifecycle method fields and blocker propagation"
      pattern: "lifecycle_method_name"
---

<objective>
Extend managed execution reporting so the real keyboard activity's startup path reaches an exact lifecycle DEX boundary.

Purpose: Once the real activity class is targeted, Linuxoid needs to report whether `onCreate` and its code item were resolved, and if not, the exact blocker instead of generic launch collapse.
Output: More precise real-activity DEX probe states and first-app-start JSON for lifecycle method lookup and execution readiness.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-managed-activity-start/02-RESEARCH.md
@.planning/phases/02-managed-activity-start/02-PATTERNS.md
@.planning/phases/02-managed-activity-start/02-01-PLAN.md
@src/apk_dex_bridge.cpp
@src/apk_native_launch.cpp
@tests/test_main.cpp
</context>

<threat_model>
<trust_boundaries>
- Untrusted staged `classes.dex` contents and code offsets
- Report JSON consumed by later execution and recovery steps
</trust_boundaries>
<stride_register>
- threat_id: T-02-03
  category: tampering
  component: lifecycle-method-resolution
  disposition: mitigate
  mitigation_plan: resolve method by exact class descriptor plus method name plus signature and reject mismatched or missing descriptors with explicit blockers
- threat_id: T-02-04
  category: denial-of-service
  component: code-item-lookup
  disposition: mitigate
  mitigation_plan: guard every code-item lookup with bounds checks and map failure to exact `entrypoint_*` or `unsupported-dex-opcode:*` blockers
</stride_register>
</threat_model>

<tasks>
<task type="auto">
  <name>Task 1: Tighten lifecycle method lookup for the real activity path</name>
  <files>src/apk_dex_bridge.cpp, include/wfa/apk_dex_bridge.hpp</files>
  <read_first>src/apk_dex_bridge.cpp, include/wfa/apk_dex_bridge.hpp, .planning/phases/02-managed-activity-start/02-RESEARCH.md</read_first>
  <action>Extend the DEX execution probe so the real keyboard activity path reports discrete states for class resolved, lifecycle method resolved, code item resolved, and exact blocker. Use the real class descriptor for `org.futo.inputmethod.latin.uix.settings.SettingsActivity`, target the `onCreate` lifecycle method first, and keep unsupported-opcode reporting precise if interpretation begins and stops immediately.</action>
  <verify>cmake --build build</verify>
  <acceptance_criteria>
    - `src/apk_dex_bridge.cpp` contains explicit state transitions more precise than `entrypoint_lookup_unresolved` for the real activity path
    - Probe fields include the real lifecycle method name and signature when method resolution succeeds
    - Unsupported or unresolved boundaries still map to exact blocker strings
  </acceptance_criteria>
  <done>DEX probe state distinguishes real activity class resolution from method/code-item resolution.</done>
</task>

<task type="auto">
  <name>Task 2: Surface real lifecycle boundary diagnostics through first-app-start JSON</name>
  <files>src/apk_native_launch.cpp, tests/test_main.cpp</files>
  <read_first>src/apk_native_launch.cpp, tests/test_main.cpp, .planning/phases/02-managed-activity-start/02-RESEARCH.md, docs/steps.md</read_first>
  <action>Thread the new real-activity DEX probe states into `first_android_app_start`, including lifecycle method name/signature, bytecode execution state, class-loading state, and exact blocker. Add regression coverage that proves the real keyboard activity path now reports a more precise lifecycle-entrypoint seam than `entrypoint_missing` or `not_attempted` when lookup work actually ran.</action>
  <verify>cmake --build build && ctest --test-dir build --output-on-failure</verify>
  <acceptance_criteria>
    - `RenderFirstAppStartJson` emits the real lifecycle method name or exact unresolved boundary for the keyboard activity path
    - `tests/test_main.cpp` asserts the real activity-start proof now reports tighter DEX/class-loading state for the keyboard APK
    - Existing fixture DEX proof tests remain present and passing
  </acceptance_criteria>
  <done>First-app-start JSON becomes precise enough to hand the next blocker to Phase 3 or Phase 4 without ambiguity.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `cmake --build build`
- [ ] `ctest --test-dir build --output-on-failure`
- [ ] Real or simulated keyboard activity proof reports lifecycle lookup state more precise than `entrypoint_missing` when lookup work executed
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The real activity-start proof names the lifecycle seam and exact blocker clearly
</success_criteria>

<output>
After completion, create `.planning/phases/02-managed-activity-start/02-02-SUMMARY.md`
</output>
