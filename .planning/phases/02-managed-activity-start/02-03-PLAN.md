---
phase: 02-managed-activity-start
plan: "03"
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - tests/test_main.cpp
  - README.md
  - CHANGELOG.md
  - docs/phased-build-plan.md
  - docs/self-healing-runtime-skeleton.md
  - docs/steps.md
  - src/project_status.cpp
autonomous: true
requirements: [APK-03, DEX-03]
user_setup: []
must_haves:
  truths:
    - "Phase 2 leaves a stable CLI proof for the real keyboard activity-start seam."
    - "Repo truth names the exact remaining blocker after real activity resolution work, not a generic launch failure."
  artifacts:
    - path: "tests/test_main.cpp"
      provides: "Hard regression coverage for real activity-start boundaries"
    - path: "README.md"
      provides: "Updated repo truth for the real keyboard activity-start checkpoint"
  key_links:
    - from: "tests/test_main.cpp"
      to: "src/apk_native_launch.cpp"
      via: "launch-apk --first-app-start-proof real-activity regression"
      pattern: "first-app-start"
---

<objective>
Lock the real keyboard activity-start checkpoint into tests, CLI proof surfaces, and repo truth.

Purpose: Phase 3 and Phase 4 need a stable, reviewable managed-activity checkpoint rather than a one-off dev observation.
Output: Hard regression coverage plus docs/status updates that name the real activity-start seam and the next blocker honestly.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-managed-activity-start/02-RESEARCH.md
@.planning/phases/02-managed-activity-start/02-PATTERNS.md
@.planning/phases/02-managed-activity-start/02-01-PLAN.md
@.planning/phases/02-managed-activity-start/02-02-PLAN.md
@README.md
@CHANGELOG.md
@docs/phased-build-plan.md
@docs/self-healing-runtime-skeleton.md
@docs/steps.md
@src/project_status.cpp
@tests/test_main.cpp
</context>

<threat_model>
<trust_boundaries>
- Human-readable docs and status output that later work may treat as runtime truth
- Real APK smoke coverage that is opt-in and local-only
</trust_boundaries>
<stride_register>
- threat_id: T-02-05
  category: spoofing
  component: repo-truth
  disposition: mitigate
  mitigation_plan: update docs only from verified proof fields and exact blockers rather than speculative runtime claims
- threat_id: T-02-06
  category: tampering
  component: regression-surface
  disposition: mitigate
  mitigation_plan: add assertions for exact real-activity field names and blocker strings so future changes cannot silently regress the checkpoint
</stride_register>
</threat_model>

<tasks>
<task type="auto">
  <name>Task 1: Add hard regression coverage for the real activity-start seam</name>
  <files>tests/test_main.cpp</files>
  <read_first>tests/test_main.cpp, .planning/phases/02-managed-activity-start/02-RESEARCH.md</read_first>
  <action>Add focused regression coverage around `launch-apk --first-app-start-proof` for the keyboard activity path. Assert that the proof names `org.futo.inputmethod.latin.uix.settings.SettingsActivity`, carries the real lifecycle method and class-loading state when available, and preserves an exact blocker when the real managed start still cannot continue. Keep the tests deterministic and offline; any real-APK smoke path must stay guarded and optional.</action>
  <verify>cmake --build build && ctest --test-dir build --output-on-failure</verify>
  <acceptance_criteria>
    - `tests/test_main.cpp` contains a regression that checks the real keyboard activity name in first-app-start output
    - `tests/test_main.cpp` checks an exact real-activity blocker or lifecycle boundary string instead of only generic launch failure text
    - The full test suite remains green
  </acceptance_criteria>
  <done>The real keyboard activity seam is pinned by automated regression coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Update docs and status with the concrete Phase 2 checkpoint and next blocker</name>
  <files>README.md, CHANGELOG.md, docs/phased-build-plan.md, docs/self-healing-runtime-skeleton.md, docs/steps.md, src/project_status.cpp</files>
  <read_first>README.md, CHANGELOG.md, docs/phased-build-plan.md, docs/self-healing-runtime-skeleton.md, docs/steps.md, src/project_status.cpp</read_first>
  <action>Update repo truth so Phase 2 states exactly what the real keyboard activity-start checkpoint now proves, whether the `SettingsActivity` class and lifecycle method are resolved from staged DEX, and what exact blocker remains next. Do not broaden the claim into full app execution or JNI/native success if those are still blocked.</action>
  <verify>rg -n \"SettingsActivity|remaining blocker|Phase 2|Managed Activity Start\" README.md CHANGELOG.md docs/phased-build-plan.md docs/self-healing-runtime-skeleton.md docs/steps.md src/project_status.cpp</verify>
  <acceptance_criteria>
    - `README.md` names the real keyboard activity-start checkpoint and its exact remaining blocker
    - `CHANGELOG.md` records the concrete Phase 2 checkpoint, not a broad architecture claim
    - `src/project_status.cpp` summarizes the current managed-activity seam truthfully
  </acceptance_criteria>
  <done>Phase 2 is auditable and hands off clearly into the next execution seam.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `cmake --build build`
- [ ] `ctest --test-dir build --output-on-failure`
- [ ] Docs and status outputs agree on the same real activity-start checkpoint and blocker
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repo truth and regression surface match the actual Phase 2 checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/02-managed-activity-start/02-03-SUMMARY.md`
</output>
