---
phase: 02-managed-activity-start
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/apk_native_launch.cpp
  - include/wfa/apk_native_launch.hpp
  - src/apk_dex_bridge.cpp
  - include/wfa/apk_dex_bridge.hpp
  - tests/test_main.cpp
autonomous: true
requirements: [APK-03, DEX-01]
user_setup: []
must_haves:
  truths:
    - "Linuxoid's first-app-start proof names `org.futo.inputmethod.latin.uix.settings.SettingsActivity` as the real managed start target for the keyboard APK."
    - "The real keyboard APK path resolves the launcher/settings class descriptor from staged DEX or reports the exact class-resolution blocker."
  artifacts:
    - path: "src/apk_native_launch.cpp"
      provides: "Real activity identity threaded into first-app-start proof"
    - path: "src/apk_dex_bridge.cpp"
      provides: "Real APK class-descriptor and lifecycle method lookup state"
  key_links:
    - from: "src/apk_native_launch.cpp"
      to: "src/apk_dex_bridge.cpp"
      via: "first-app-start proof target activity descriptor and DEX lookup"
      pattern: "first_android_app_start"
---

<objective>
Bind the real keyboard APK launcher/settings activity into Linuxoid's first-app-start proof and DEX lookup seam.

Purpose: Phase 2 must stop speaking only in fixture `MainActivity` terms and instead target the real `org.futo.inputmethod.latin.uix.settings.SettingsActivity` path.
Output: Updated proof/report fields plus DEX lookup state that name the real activity class or an exact class-resolution blocker.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-keyboard-apk-intake/01-03-SUMMARY.md
@.planning/phases/02-managed-activity-start/02-RESEARCH.md
@.planning/phases/02-managed-activity-start/02-PATTERNS.md
@src/apk_native_launch.cpp
@src/apk_dex_bridge.cpp
@tests/test_main.cpp
</context>

<threat_model>
<trust_boundaries>
- Untrusted APK manifest and DEX payloads from `/home/astra/Downloads/keyboard-0.1.28.apk`
- User-supplied `--package` and `--component` CLI inputs
- Persisted staged JSON artifacts under the Linuxoid session root
</trust_boundaries>
<stride_register>
- threat_id: T-02-01
  category: tampering
  component: activity-target-resolution
  disposition: mitigate
  mitigation_plan: normalize package and component identity, require the resolved class descriptor to match the selected package before threading it into first-app-start proof fields
- threat_id: T-02-02
  category: denial-of-service
  component: dex-class-lookup
  disposition: mitigate
  mitigation_plan: preserve bounds-checked lookup and return an exact class-resolution blocker instead of crashing or looping on malformed DEX metadata
</stride_register>
</threat_model>

<tasks>
<task type="auto">
  <name>Task 1: Thread real keyboard activity identity into first-app-start proof</name>
  <files>src/apk_native_launch.cpp, include/wfa/apk_native_launch.hpp</files>
  <read_first>src/apk_native_launch.cpp, include/wfa/apk_native_launch.hpp, .planning/phases/02-managed-activity-start/02-RESEARCH.md</read_first>
  <action>Update the first-app-start proof builder so the real keyboard APK path keeps `org.futo.inputmethod.latin/.uix.settings.SettingsActivity` and `org.futo.inputmethod.latin.uix.settings.SettingsActivity` as the target activity/component when intent resolution succeeded, even when `launch_status` is still `libraries_failed_to_load`. Add any new struct fields needed to report the real target activity descriptor and its resolution state without replacing the existing fixture-path fields.</action>
  <verify>cmake --build build</verify>
  <acceptance_criteria>
    - `src/apk_native_launch.cpp` contains `org.futo.inputmethod.latin.uix.settings.SettingsActivity` handling only via resolved report fields, not hard-coded fixture fallback names
    - `RenderFirstAppStartJson` emits a deterministic field for the real activity target resolution state
    - Existing fixture first-app-start fields remain present in the JSON renderer
  </acceptance_criteria>
  <done>First-app-start proof can name the real keyboard activity target without claiming the app started.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve the real activity descriptor into staged DEX lookup state</name>
  <files>src/apk_dex_bridge.cpp, include/wfa/apk_dex_bridge.hpp, src/apk_native_launch.cpp</files>
  <read_first>src/apk_dex_bridge.cpp, include/wfa/apk_dex_bridge.hpp, src/apk_native_launch.cpp, .planning/phases/02-managed-activity-start/02-RESEARCH.md, docs/steps.md</read_first>
  <action>Extend the DEX execution-probe setup so the real activity class descriptor derived from `org.futo.inputmethod.latin.uix.settings.SettingsActivity` is attempted before the proof falls back to generic `dex_unavailable` or `entrypoint_lookup_unresolved`. Preserve exact states for class not found, lifecycle method not found, or code item unavailable. Do not fake bytecode execution if the real class or `onCreate` lookup still fails.</action>
  <verify>cmake --build build && ctest --test-dir build --output-on-failure</verify>
  <acceptance_criteria>
    - `src/apk_dex_bridge.cpp` emits a deterministic probe state for real activity class lookup success or failure
    - `src/apk_native_launch.cpp` no longer maps the keyboard APK first-app-start seam directly to `dex_unavailable` when class lookup was attempted
    - `tests/test_main.cpp` contains a regression that asserts the real keyboard activity name or exact class-resolution blocker in first-app-start JSON
  </acceptance_criteria>
  <done>The real keyboard activity becomes the explicit DEX lookup target, with honest exact blockers if lookup still fails.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `cmake --build build`
- [ ] `ctest --test-dir build --output-on-failure`
- [ ] Real or fixture-backed first-app-start output names `org.futo.inputmethod.latin.uix.settings.SettingsActivity` or an exact class-resolution blocker instead of a generic missing-entrypoint state
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The first-app-start proof targets the real keyboard APK activity path explicitly
</success_criteria>

<output>
After completion, create `.planning/phases/02-managed-activity-start/02-01-SUMMARY.md`
</output>
