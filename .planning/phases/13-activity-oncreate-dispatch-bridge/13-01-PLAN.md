---
phase: 13-activity-oncreate-dispatch-bridge
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/native_execute_stub.cpp
  - include/wfa/native_execute_stub.hpp
  - src/apk_dex_bridge.cpp
  - include/wfa/apk_dex_bridge.hpp
  - src/apk_native_launch.cpp
  - include/wfa/apk_native_launch.hpp
  - tests/test_main.cpp
autonomous: true
requirements: [JNI-15]
user_setup: []
must_haves:
  truths:
    - "Linuxoid no longer stops at the generic `activity_oncreate_bundle_dispatch_required` seam for `libjni_latinime.so`."
    - "The real keyboard APK path executes or precisely attempts `SettingsActivity->onCreate(Landroid/os/Bundle;)V` inside the bound Linuxoid runtime context."
  artifacts:
    - path: "src/native_execute_stub.cpp"
      provides: "Linuxoid-owned lifecycle dispatch attempt beyond the bound runtime-context seam"
    - path: "src/apk_native_launch.cpp"
      provides: "Operator-facing launch truth for the new post-dispatch seam"
  key_links:
    - from: "src/native_execute_stub.cpp"
      to: "src/apk_native_launch.cpp"
      via: "lifecycle dispatch report and launch status"
      pattern: "activity_oncreate_bundle_dispatch_required"
---

<objective>
Close the first real `Activity.onCreate(Bundle)` dispatch gap for the keyboard APK.

Purpose: Move Linuxoid from a completed runtime-context binding seam into a real lifecycle dispatch attempt for `SettingsActivity->onCreate(Landroid/os/Bundle;)V`.
Output: A smaller, exact seam than the current `activity_oncreate_bundle_dispatch_required` boundary.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-activity-oncreate-dispatch-bridge/13-CONTEXT.md
@.planning/phases/13-activity-oncreate-dispatch-bridge/13-RESEARCH.md
@.planning/phases/13-activity-oncreate-dispatch-bridge/13-PATTERNS.md
@src/native_execute_stub.cpp
@src/apk_dex_bridge.cpp
@src/apk_native_launch.cpp
@tests/test_main.cpp
</context>

<threat_model>
<trust_boundaries>
- Bound Linuxoid runtime context crossing into real lifecycle dispatch reporting
</trust_boundaries>
<stride_register>
- threat_id: T-13-01
  category: spoofing
  component: lifecycle-dispatch-bridge
  disposition: mitigate
  mitigation_plan: keep any lifecycle dispatch attempt explicit, deterministic, and limited to the minimum surface needed to move the real keyboard seam
</stride_register>
</threat_model>

<tasks>
<task type="auto">
  <name>Task 1: Move beyond generic post-binding dispatch failure for `libjni_latinime.so`</name>
  <files>src/native_execute_stub.cpp, include/wfa/native_execute_stub.hpp, src/apk_dex_bridge.cpp, include/wfa/apk_dex_bridge.hpp, src/apk_native_launch.cpp, include/wfa/apk_native_launch.hpp, tests/test_main.cpp</files>
  <read_first>src/native_execute_stub.cpp, src/apk_dex_bridge.cpp, src/apk_native_launch.cpp, .planning/phases/13-activity-oncreate-dispatch-bridge/13-RESEARCH.md</read_first>
  <action>Extend or tighten the native execute and first-app-start path so the JNI-registered primary library and selected keyboard lifecycle target can advance beyond the current `activity_oncreate_bundle_dispatch_required` state into a Linuxoid-owned `Activity.onCreate(Bundle)` dispatch attempt or a smaller exact lifecycle seam. Preserve deterministic reporting and do not claim visible launch success if Linuxoid still lacks the required post-dispatch bridge.</action>
  <verify>TMPDIR=/home/astra/codex/wine-for-android/.tmp cmake --build build</verify>
  <acceptance_criteria>
    - the real keyboard APK seam is smaller than the current `activity_oncreate_bundle_dispatch_required` blocker
    - lifecycle dispatch reporting stays deterministic
    - fixture and keyboard-identity dispatch coverage stay offline and green
  </acceptance_criteria>
  <done>Linuxoid reaches a smaller lifecycle-dispatch seam for the real keyboard APK.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `TMPDIR=/home/astra/codex/wine-for-android/.tmp cmake --build build`
- [ ] `TMPDIR=/home/astra/codex/wine-for-android/.tmp ctest --test-dir build --output-on-failure`
- [ ] Real or representative launch JSON shows a smaller app-start blocker than the current `activity_oncreate_bundle_dispatch_required` failure
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Linuxoid narrows the real keyboard APK lifecycle-dispatch seam without pretending full visible-launch success
</success_criteria>

<output>
After completion, create `.planning/phases/13-activity-oncreate-dispatch-bridge/13-01-SUMMARY.md`
</output>
