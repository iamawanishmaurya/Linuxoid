#include "wfa/project_status.hpp"

#include <cmath>
#include <sstream>

namespace wfa {

std::vector<PhaseStatus> BuildDefaultPhases() {
  return {
      {"P1", "Research and architecture lock", 100,
       "Language, runtime direction, and scope are locked."},
      {"P2", "Build and tooling scaffold", 100,
       "CMake, CLI entrypoint, and local test target exist."},
      {"P3", "Package and storage contract", 100,
       "APK layout and host storage mapping are encoded in code."},
      {"P4", "Runtime and service contract", 100,
       "The project can load an APK into a compat root, install it on a live target, provision its IME service, inspect attached-target package metadata, auto-resolve simple launcher components, route installed-package launch through a backend-neutral contract, preflight attached Android targets before launch, materialize a Linuxoid-owned native bundle plan plus bootstrap spec for simple foreground APKs, emit a Linuxoid-owned native bootstrap manifest plus entrypoint stub for those candidates, hand that bootstrap off to a Linuxoid-owned lifecycle and service shim with deterministic session artifacts, expose a Linuxoid-owned local `native` runtime bridge for staged target discovery plus staged package inspection plus staged-package preflight plus bootstrap-execution handoff, expose a direct native-only `launch-apk` slice that stages ABI-matching libraries plus assets into a deterministic session directory and proves `JNI_OnLoad` plus minimal native activity execution through structured JSON, extend that same APK-launched session path into deterministic native surface/session proof plus deterministic asset/resource bridge proof with session-bound asset listing, staged asset reads, metadata-only `resources.arsc` plus `res/` inspection, deterministic lifecycle/looper/input proof with ordered activity states plus synthetic event dispatch, a session-bound DEX/bootstrap probe that stages `classes.dex` entries, parses safe DEX header plus string/type/proto/method/class metadata, and emits a truthful `art_bootstrap` contract without claiming Java/Kotlin execution, a session-bound PackageManager plus MAIN/LAUNCHER intent plus activity-launch contract that writes deterministic package-record, intent-resolution, and activity-launch artifacts, captures labels plus exported/enabled flags plus manifest intent filters, supports explicit component requests, a session-bound permissions plus AppOps contract that parses plain-XML `uses-permission` entries, materializes deterministic requested plus granted plus denied permission state, and emits stable operation records for storage-sensitive, Binder/service, activity, and DEX/runtime access, and now **P8 Self-Healing Android Device Watchdog + Recovery Loop**, which consumes those same session-bound health fields, chooses deterministic local recovery actions, and writes stable watchdog plus recovery-journal artifacts, expose a Linuxoid-owned Binder-shaped service-registry foundation with deterministic package-manager, activity-manager, app-local placeholder, missing-service lookup, and socketpair-backed transport artifacts, inspect APK manifest plus asset/resource readiness through a native Linux bridge that is staged for future ART/DEX loading, classify runtime health plus deterministic recovery actions for the Self-Healing Android Device skeleton, formalize those recovery actions with stable rank, retry-budget, and scope metadata, write a deterministic ART/classloader preparation plan from staged APK dex entries plus manifest targets, materialize a manifest-derived application-plus-activity bootstrap planning seam on top of class-resolution plus runtime-smoke evidence, upgrade that seam into a deterministic supervised bootstrap-execution contract with stable execution-context, runner-script, runner-state, and phase-log artifacts, and now keep the live health plus replay path fast enough to rerun on staged bundles by reusing one opened APK archive plus the already-staged bundle manifest."},
      {"P5", "Graphics and host integration", 84,
       "Linux launcher artifacts now target a backend-neutral installed-package launch seam, and both Waydroid-backed and attached-ADB host launch verification can reuse the same generated launcher flow."},
      {"P6", "APK execution and validation", 93,
       "A live Linux host launcher now proves the local keyboard and F-Droid APK flows plus Calculator, Settings, and installed-package F-Droid launch flows on Waydroid, attached-ADB now supports metadata-backed auto-resolution and a live `3/3` installed-package verification matrix without Waydroid-shaped product names, Calculator passes a real local `plan-native-spike` verification with a written native bootstrap spec and no blockers, the direct `launch-apk` path now proves native launch plus surface plus asset/resource plus lifecycle/input plus DEX/bootstrap preparation on fixture APKs, and the generated Linuxoid-native lifecycle shim now runs locally with a ready session handoff while still reporting its pending execution-engine state honestly."},
  };
}

std::vector<Checkpoint> BuildDefaultCheckpoints() {
  return {
      {"C1", "Environment Reproducibility", 15,
       CompletionState::kInValidation,
       "Build instructions and the local scaffold exist; clean-room proof is pending."},
      {"C2", "Golden App Launch", 20, CompletionState::kComplete,
       "The keyboard APK now installs, enables, sets default IME, and launches settings successfully on the live target."},
      {"C3", "Representative Compatibility Set", 25,
       CompletionState::kInValidation,
       "The keyboard, Calculator, Settings, and F-Droid launch paths are now verified on live runtimes, and Calculator also passes the first native spike planner, but the wider native app matrix is still incomplete."},
      {"C4", "Host Integration", 20, CompletionState::kInValidation,
       "Linux wrappers and `.desktop` entries now launch APK-backed and multiple installed Waydroid apps from the host, and Linuxoid now owns a local native bootstrap stub for Calculator-like bundles, but compositor, clipboard, and richer input handoff still need proof."},
      {"C5", "Repeatability and Regression Guard", 20,
       CompletionState::kComplete,
       "The build, tests, installed-package verification, and live native Calculator spike planner all passed again on the latest slice."},
  };
}

int CalculateAveragePhaseProgress(std::span<const PhaseStatus> phases) {
  if (phases.empty()) {
    return 0;
  }

  int total = 0;
  for (const auto& phase : phases) {
    total += phase.progress;
  }

  return static_cast<int>(
      std::lround(static_cast<double>(total) / phases.size()));
}

std::string RenderProjectStatusReport() {
  const auto phases = BuildDefaultPhases();
  const auto checkpoints = BuildDefaultCheckpoints();
  const int phase_progress = CalculateAveragePhaseProgress(phases);
  const int checkpoint_progress =
      CalculateWeightedCheckpointProgress(checkpoints);
  const int completed_checkpoints = CountCompletedCheckpoints(checkpoints);

  std::ostringstream output;
  output << "Project: Linuxoid\n";
  output << "Language: C++\n";
  output << "Runtime Strategy: backend-neutral attached Android targets on the path to a native Linux compatibility layer\n";
  output << "Scaffold Readiness: " << RenderLoadingBar(phase_progress) << "\n";
  output << "Native Execution Readiness: " << RenderLoadingBar(98) << "\n";
  output << "Execution Focus: P0 Freeze & Triage -> P1 NDK Execution Core -> P2 Window + Graphics\n";
  output << "Current Direct Runtime Phase: First DEX App-Method Invocation Checkpoint\n";
  output << "Next Phase: P16 Managed Bytecode Invocation + ActivityThread Contract (bridge MainActivity onCreate into a real ART-owned runtime context)\n";
  output << "First App Start Checkpoint: `launch-apk --first-app-start-proof` now parses real DEX tables, resolves `MainActivity` from staged DEX metadata, materializes a deterministic lifecycle receiver placeholder in `v0`, executes a tiny app-local `linuxoidComputeValue()I` helper through `invoke-direct` plus `move-result`, crosses a stubbed `android.app.Activity.onCreate()V` framework boundary, allocates a placeholder `StateCarrier` object, executes deterministic `iput` plus `iget` access for `value:I`, reaches a real DEX `return` with value `1`, and reports the next blocker as `bridge_activity_oncreate_into_real_art_runtime_context`.\n";
  output << "Legacy Scaffold Phases:\n";

  for (const auto& phase : phases) {
    output << "  " << phase.id << " " << phase.name << " "
           << RenderLoadingBar(phase.progress, 10) << '\n';
  }

  output << "Checkpoint Gates: " << checkpoint_progress << "/100"
         << ", " << completed_checkpoints << "/5 passed\n";
  for (const auto& checkpoint : checkpoints) {
    output << "  " << checkpoint.id << " " << checkpoint.name << " "
           << RenderLoadingBar(static_cast<int>(checkpoint.state), 10) << '\n';
  }

  return output.str();
}

std::string RenderPackageLayoutReport(const PackageLayout& layout) {
  std::ostringstream output;
  output << "Host Package Root: " << layout.host_package_root << '\n';
  output << "Host Manifest: " << layout.host_manifest_path << '\n';
  output << "Host Data Root: " << layout.host_data_root << '\n';
  output << "Host External Data Root: " << layout.host_external_data_root << '\n';
  output << "Host OBB Root: " << layout.host_obb_root << '\n';
  output << "Guest Base APK: " << layout.guest_base_apk << '\n';
  output << "Guest Private Data Root: " << layout.guest_private_data_root << '\n';
  output << "Guest External Data Root: " << layout.guest_external_data_root << '\n';
  output << "Guest OBB Root: " << layout.guest_obb_root << '\n';
  return output.str();
}

std::string DescribeMvpFoundation() {
  std::ostringstream output;
  output << "Project: Linuxoid\n";
  output << "Chosen Language: C++\n";
  output << "Alternative: Rust for later helper services\n";
  output << "Runtime Direction: backend-neutral attached Android targets on the path to a native Linux compatibility layer\n";
  output << "Current State: scaffold 96/100, execution 98/100\n";
  output << "Execution Focus: P0 Freeze & Triage -> P1 NDK Execution Core -> P2 Window + Graphics\n";
  output << "Current Slice: checkpoint engine, package layout planner, manifest/runtime assessor, runtime bridges, backend-neutral installed-package launch plus target discovery/preflight, attached-target package inspection and launcher resolution, Linux desktop launch artifacts with repeatable generic per-app, matrix, and APK-backed verification, the first native spike planner that writes a Linuxoid-owned bundle layout and bootstrap spec for simple APKs, stages host-ABI native libraries plus extracted assets/resources into deterministic bundle roots, a direct native-only `launch-apk` slice that stages ABI-matching libraries plus assets into a deterministic session directory, calls `JNI_OnLoad` when present, creates a minimal native activity/session object, emits structured JSON for success or failure, and now extends that same session path into deterministic native surface/session proof plus deterministic asset/resource bridge proof with session-bound asset listing, staged asset reads, metadata-only `resources.arsc` plus `res/` inspection, explicit `asset_health` plus `resource_health` reporting, deterministic lifecycle/looper/input proof with ordered `created -> started -> resumed -> paused -> stopped -> destroyed` state transitions plus synthetic touch/key dispatch, and a session-bound DEX/bootstrap probe that stages `classes.dex` entries, parses safe DEX header-and-count metadata, records deterministic checksums plus staged paths, and emits a truthful `art_bootstrap` contract with `class_loader_ready` while keeping `java_execution_supported: false`, a Linuxoid-owned native bootstrap surface that emits a manifest, env script, and local entrypoint stub, the first lifecycle/service shim that turns that bootstrap into deterministic session artifacts and service bindings, a Linuxoid-owned local `native` runtime bridge that now exposes staged target discovery plus staged package inspection plus staged-package preflight plus bootstrap-execution handoff instead of a hard stub, a hardened native runner that now `execve`s a controlled child process, sets deterministic cwd plus Linuxoid-only environment variables, closes inherited file descriptors, loads native libraries in deterministic order, calls `JNI_OnLoad`, and emits structured JSON for harnesses, plus headless `ANativeWindow`-shaped first-pixel and callback fixtures, the first real optional Wayland client surface proof, a real optional EGL context plus pbuffer smoke gate, a minimal `ANativeWindow` bridge contract with deterministic geometry updates and stable metadata artifacts, a focused input queue fixture with deterministic pointer/key injection plus focus-ownership artifacts, a Linuxoid-owned Binder-shaped service-registry foundation with deterministic registration, lookup, missing-service truth, app-local placeholder support, limitation flags, and a socketpair-backed local transport seam, a new APK resource-readiness bridge that can inspect manifest plus asset metadata from a plain APK/ZIP path before ART/DEX loading exists, a self-healing runtime health skeleton that records subsystem health, chooses bounded recovery actions, emits replayable trace artifacts, writes first-class deterministic recovery-plan files, formalizes those actions with stable rank plus retry-budget plus recovery-scope metadata, now exposes summary fields like dependency-blocked state plus failing-subsystem counts plus recovery-action counts directly in the health JSON plus an explicit six-area `core_subsystem_records` projection for APK staging, native loading, surface, input, Binder, and DEX/classloader readiness plus an explicit four-scenario `canonical_recovery_scenarios` contract for missing artifact, failed native load, unavailable display, and failed service lookup, now also exposes the seven-source replay bundle through explicit `trace_bundle_complete`, canonical trace-source counts and names, direct health-replay paths, and a public ART probe inventory plus detection-reason plus capability contract, writes a deterministic diagnostic trace index with per-source fingerprints and event boundaries plus a one-shot replayable diagnostic fixture command, now treats application/activity bootstrap planning as a first-class health and replay seam, upgrades execution into its own deterministic supervised bootstrap-execution contract with stable execution-context, runner-script, runner-state, and phase-log artifacts, and keeps live health plus replay fast enough to rerun on staged bundles by reusing one opened APK archive plus the already-staged bundle manifest, alongside a deterministic ART/classloader preparation fixture that inventories dex entries, normalizes manifest target classes, writes classpath artifacts, records host ART probe candidates and selection rationale in `art-runtime-probe-inventory.json`, classifies the selected probe as bootstrap-capable or detection-only across `dalvikvm` or `dalvikvm64` and `app_process` or `app_process64`, resolves manifest-target descriptors from real staged DEX contents, attempts a real host-side ART class-resolution probe when that path is safely available while still reporting runtime absence honestly on hosts without ART, can now route that runtime-smoke plus bootstrap-execution seam through an explicit Linuxoid-owned ART probe override for deterministic fixture runs, upgrades the bootstrap seam into an application-plus-launcher attempt contract that only trusts a real manifest application class when one is declared, and now persists **P10 Android Permissions + AppOps Contract** as a sandbox-backed session contract with deterministic schema, user/app identity, healing metadata, repeated-launch validation, explicit no-silent-dangerous-grant behavior, and a focused `inspect-apk-permissions` CLI surface, **P11 Minimal ActivityManager/ProcessManager Contract** as a sandbox-backed session contract with deterministic `activity_manager` plus `process_manager` artifacts, Linuxoid-owned process identity placeholders, deterministic start plus restart policy metadata, focused `inspect-apk-process` inspection, and watchdog-driven `rebuild_process_manager_state` recovery, **P12 WindowManager + Wayland/EGL Surface Contract** as a sandbox-backed session contract with deterministic `window_manager` artifacts, explicit surface lifecycle states, focused `inspect-apk-window` inspection, and watchdog-driven `rebuild_window_manager_state` recovery, **P13 Real ART Runtime Path / Java VM Bootstrap Contract** as a sandbox-backed session contract with deterministic `runtime_bridge` artifacts, runtime-root discovery plus boot classpath plus runtime-library inputs, focused `inspect-apk-runtime` inspection, explicit runtime bootstrap states, and watchdog-driven `retry_runtime_bootstrap` recovery, **P14 Java/Kotlin APK Proof Contract** as a sandbox-backed session contract with deterministic `java_apk_proof` artifacts, focused `inspect-apk-java` inspection, explicit bootstrap-and-lifecycle wiring proof, and watchdog-visible `java_proof_health`, and **P15 Third-Party APK Compatibility Sprint** as a deterministic compatibility-report layer with `inspect-apk-compatibility` plus `inspect-apk-compatibility-suite`, locally generated Java-style, permission-heavy, and native/JNI fixture profiles, and stable compatibility artifact emission across supported, partial, blocked, missing-runtime, missing-surface, missing-native-lib, needs-real-art, recovered, and degraded states; this is the contract P16 Managed Bytecode Invocation + ActivityThread Contract should build on for real ART-owned class loading, managed-thread bootstrap, and bytecode invocation gating.\n";
  output << "Execution Baseline: the FUTO keyboard APK still returns Ready for typing: yes through runtime-backed verification, attached-ADB still verifies a live Settings, Calculator, and F-Droid matrix, Calculator passes `plan-native-spike` with a real native bundle plan and no blockers, the generated Linuxoid-native lifecycle shim now runs locally with `Lifecycle Handoff Ready: yes`, the `native` runtime bridge now reports a real local `linuxoid-native` target, can inspect staged package metadata from a compat root, can preflight staged launcher readiness without attached ADB or Waydroid, now blocks that preflight honestly when host ART is missing, when only a detection-only host `app_process` or `app_process64` surface is present, or when the staged package cannot produce a real native spike candidate path, now treats host `dalvikvm64` the same as host `dalvikvm` for bootstrap-capable probing, now surfaces both the selected recovery actions and the canonical four-scenario recovery mapping directly in native preflight and launch reports, can hand staged candidate packages into the supervised bootstrap-execution seam while failing honestly for non-candidate bundles, and now surfaces bootstrap, runtime-health, recovery-plan, replay artifact paths, ART probe inventory paths, ART probe detection reasons, ART probe capability, and blocked-subsystem summaries directly in the public native launch report, the direct `launch-apk --activity-proof` path now materializes a Linuxoid-owned package record with labels plus exported/enabled flags plus manifest intent filters, resolves either a `MAIN` plus `LAUNCHER` component or an explicit component request, records deterministic blocked reasons like `package_not_found`, `no_launcher_activity`, `ambiguous_launcher_activities`, and `unsupported_component_type`, writes deterministic activity-launch artifacts, and ties that activity-launch contract back to lifecycle, surface, input, Binder, and DEX/bootstrap readiness without pretending full Android framework startup already exists, the new `launch-apk --storage-proof` path now implements **P9 Android App Storage + Sandbox Contract** with deterministic `sandbox/data/data/<package>`-style app directories, `files` plus `cache` roots, marker-file proof, safe path resolution, explicit `storage_health` plus `sandbox_health`, and honest `path_sandbox_only` isolation, the new `launch-apk --permissions-proof` path now implements **P10 Android Permissions + AppOps Contract** with sandbox-backed persisted permission/AppOps state, deterministic requested plus granted plus denied permission state, stable AppOps records for storage-sensitive, audio capture placeholder, Binder/service, activity, and DEX/runtime access, explicit `permission_health` plus `app_ops_health`, deterministic healing for missing, malformed, incomplete, stale, or incompatible contract files, a focused `inspect-apk-permissions` operator path, and no silent dangerous permission grants, the new `launch-apk --process-proof` path now implements **P11 Minimal ActivityManager/ProcessManager Contract** with sandbox-backed `activity_manager` plus `process_manager` state, deterministic process identity plus lifecycle metadata, focused `inspect-apk-process` inspection, and explicit `activity_manager_health` plus `process_health`, the new `launch-apk --window-proof` path now implements **P12 WindowManager + Wayland/EGL Surface Contract** with sandbox-backed `window_manager` state, deterministic activity/process/surface mapping, focused `inspect-apk-window` inspection, and explicit `window_health`, the new `launch-apk --runtime-proof` path now implements **P13 Real ART Runtime Path / Java VM Bootstrap Contract** with sandbox-backed `runtime_bridge` state, deterministic runtime-root discovery plus boot classpath plus runtime-library inputs, focused `inspect-apk-runtime` inspection, explicit `runtime_health`, honest `java_execution_supported: false`, and deterministic runtime bootstrap states, the new `launch-apk --java-proof` path now implements **P14 Java/Kotlin APK Proof Contract** with sandbox-backed `java_apk_proof` state, focused `inspect-apk-java` inspection, deterministic package/activity/process/window/runtime session mapping, honest `java_execution_supported: false`, and explicit `java_proof_health`, the new `inspect-apk-compatibility` plus `inspect-apk-compatibility-suite` path now implements **P15 Third-Party APK Compatibility Sprint** with real bridge-backed compatibility classification for Java-style, permission-heavy, and native/JNI fixtures, deterministic compatibility artifacts under `sandbox/data/data/<package>/compatibility`, and Self-Healing Android Device recovery diagnostics for blocked or partial third-party-style APKs, and the `launch-apk --self-heal-proof` path now consumes those process, window, runtime, Java proof, and compatibility-adjacent readiness fields too so the Self-Healing Android Device watchdog can record explicit attempted/succeeded/failed `rebuild_process_manager_state`, `rebuild_window_manager_state`, `retry_runtime_bootstrap`, and Java-proof rebuild diagnostics alongside `rebuild_permission_state`, `repair_app_storage`, `restage_assets`, `restart_surface`, `refresh_binder_services`, `rebuild_dex_bootstrap`, and `rerun_intent_resolution`, the dex-only Calculator bundle still fails honestly with structured `no_native_libraries_found` output when forced through the native-loader runner, but the self-healing runtime now classifies that same staged dex-only bundle as `native_loading: not_required` instead of treating the absence of `.so` payloads as a false native-loader failure, the planner now reports unsupported ABI libraries without staging them as runnable, the asset bridge can now list normalized asset paths from both staged roots and plain APK/ZIP fixtures, reject traversal, and feed a structured `inspect-apk-resources` readiness report, the hardened native runner still passes the five-second gate against a fixture shared library that exports both `JNI_OnLoad` and `ANativeActivity_onCreate`, the `native-art-classloader-fixture` command now proves Linuxoid can inventory APK dex entries, normalize application plus activity target classes, write deterministic classloader-plan artifacts, record deterministic ART probe candidate inventory plus selection rationale, classify the selected probe as bootstrap-capable or detection-only, and report missing host ART honestly, the `native-art-class-resolution-fixture` command now proves Linuxoid can resolve manifest-target descriptors from real staged DEX contents and write deterministic resolution-map plus trace artifacts without pretending ART already executed them, the `native-art-runtime-smoke` command now proves Linuxoid can select a deterministic manifest-derived class target, carry offline class resolution into deterministic invocation-plan, runtime-log, JSONL trace, and ART probe inventory artifacts, distinguish detection-only host `app_process` or `app_process64` paths from bootstrap-capable host `dalvikvm` or `dalvikvm64` paths, and attempt a real host-side ART class-resolution probe when that path is safely available while still reporting runtime absence honestly on this host, while Linuxoid-owned override probes can now exercise that same runtime-smoke path deterministically in fixtures, and the new direct runtime bridge now turns that discovery plus bootstrap planning seam into a session-bound `runtime_bridge` contract with deterministic runtime handle plus boot classpath plus native library directory plus dex-file metadata while still keeping full bytecode execution explicitly out of scope; the immediate handoff is that P16 Managed Bytecode Invocation + ActivityThread Contract should start from these existing package/activity/storage/permission/AppOps/process/window/runtime/java-proof/compatibility contracts and convert them into real ART-owned class loading, ActivityThread bootstrap, managed bytecode invocation, and process ownership without bypassing the current Self-Healing Android Device health gates.\n";
  output << "Why: this keeps the scaffold useful while turning the first direct-execution slice into something measurable instead of hypothetical.\n";
  return output.str();
}

}  // namespace wfa
